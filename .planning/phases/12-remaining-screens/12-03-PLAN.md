---
phase: 12-remaining-screens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/family/index.tsx
  - app/(tabs)/family/[id].tsx
  - app/(tabs)/invite/[token].tsx
autonomous: true
requirements: [SCREEN-06, SCREEN-10]

must_haves:
  truths:
    - "Family list screen shows user's families in a responsive layout matching cookbook.pen"
    - "Family detail screen shows members, roles, invite controls, and admin actions matching cookbook.pen"
    - "Invite screen renders responsively and supports native share sheet for sending invite links"
    - "Invite acceptance handles both existing users (direct join) and new users (redirect to signup with token)"
    - "All three screens adapt to mobile, tablet, and web breakpoints"
  artifacts:
    - path: "app/(tabs)/family/index.tsx"
      provides: "Responsive family list screen"
    - path: "app/(tabs)/family/[id].tsx"
      provides: "Responsive family detail with member management"
    - path: "app/(tabs)/invite/[token].tsx"
      provides: "Responsive invite acceptance screen with share sheet"
  key_links:
    - from: "app/(tabs)/family/[id].tsx"
      to: "src/features/family/api.ts"
      via: "import family management functions"
      pattern: "listFamilies|createFamily"
    - from: "app/(tabs)/invite/[token].tsx"
      to: "react-native Share API"
      via: "Share.share for native share sheet"
      pattern: "Share\\.share"
---

<objective>
Rebuild family management screens and invite screen to match cookbook.pen at all 3 breakpoints. Add native share sheet for invite links.

Purpose: Family management is core to the app's trust proposition. Invite flow must handle both existing and new users smoothly.
Output: Responsive family list, family detail with member management, and invite screen with share sheet.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-remaining-screens/12-CONTEXT.md
@.planning/phases/12-remaining-screens/12-RESEARCH.md
@cookbook.pen
@app/(tabs)/family/index.tsx
@app/(tabs)/family/[id].tsx
@app/(tabs)/family/_layout.tsx
@app/(tabs)/invite/[token].tsx
@src/features/family/api.ts
@src/lib/tokens.ts
@src/lib/hooks/useBreakpoint.ts
@src/components/nav/PageContainer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild family list and family detail screens</name>
  <files>app/(tabs)/family/index.tsx, app/(tabs)/family/[id].tsx</files>
  <action>
**Family list (`index.tsx`):**
Rebuild to match cookbook.pen. Preserve existing listFamilies data fetching.

- PageContainer wrapper
- useBreakpoint() for responsive layout
- Header: "Family" title with fontFamilyDisplay tokens
- "Create Family" button (Pressable, accentBlue bg, radiusMd)
- List of family cards:
  - Mobile: single column, full width cards
  - Tablet/Web: cards with maxWidth, centered or grid layout per .pen
  - Each card: family name, member count, Pressable navigating to [id]
  - bgCard background, radiusMd, shadowSm, padding 16
- Empty state: "No families yet" + Create Family CTA
- Loading: ActivityIndicator

**Family detail (`[id].tsx`):**
Rebuild to match cookbook.pen. Preserve ALL existing functionality: member list, roles display, invite member, remove member, transfer ownership, leave family, delete family.

- PageContainer wrapper
- useBreakpoint() for responsive layout
- Header: family name (fontFamilyDisplay, fontSize2xl)
- Member list section:
  - Each member card: avatar placeholder (initials circle), display name, role badge (owner/admin/member)
  - Responsive: stack on mobile, wider cards on tablet/web
  - Admin actions per member: remove, change role (only visible to owner/admin)
- Invite section:
  - "Invite Member" button that generates invite link
  - Uses Share.share() from react-native to open native share sheet
  - Clipboard fallback: copy invite link using expo-clipboard if share fails
  - Per CONTEXT.md: link sharing primary, optional email entry below
- Admin controls section (only visible to owner/admin):
  - Transfer ownership, Delete family — destructive actions with confirmation alerts
- Leave family option for non-owners

All colors from tokens. All fonts from tokens. No StyleSheet.create for breakpoint-dependent styles. Pressable not TouchableOpacity.
  </action>
  <verify>
    <automated>npx tsc --noEmit app/\(tabs\)/family/index.tsx app/\(tabs\)/family/\[id\].tsx 2>&1 | head -20</automated>
  </verify>
  <done>Family list shows responsive cards. Family detail shows member list with roles, invite via share sheet, admin controls. All existing functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild invite acceptance screen</name>
  <files>app/(tabs)/invite/[token].tsx</files>
  <action>
Rebuild invite acceptance screen to match cookbook.pen. Preserve existing invite token acceptance logic.

- PageContainer wrapper
- useBreakpoint() for responsive layout
- Centered card layout:
  - Mobile: full-width with padding 24
  - Tablet/Web: centered card with maxWidth 480, shadowMd, radiusMd, bgCard
- Content:
  - App logo or family icon at top
  - "You've been invited to join [family name]!" heading (fontFamilyDisplay)
  - Inviter info if available
  - "Accept Invite" primary button (accentBlue bg, white text, full width, radiusMd)
  - "Decline" secondary button (outline style)
- Dual-path handling (preserve existing logic):
  - If user is authenticated: accept invite directly, navigate to family detail
  - If user is not authenticated: show "Sign Up to Join" and "Already have an account? Sign In" links that preserve the invite token in the `next` parameter
- Loading state while checking invite validity
- Error states: expired invite, already accepted, invalid token
- Success state: "You've joined [family name]!" with navigation to family

All tokens, useBreakpoint, Pressable. No hardcoded colors.
  </action>
  <verify>
    <automated>npx tsc --noEmit app/\(tabs\)/invite/\[token\].tsx 2>&1 | head -20</automated>
  </verify>
  <done>Invite screen renders responsively with centered card. Handles authenticated (direct join) and unauthenticated (redirect to signup with token) paths. All styling uses tokens.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all modified files
- Family list shows responsive layout
- Family detail shows members, roles, invite share, admin controls
- Invite screen handles both auth states correctly
</verification>

<success_criteria>
Family and invite screens match cookbook.pen at all breakpoints. Member management, invite sharing (native share sheet + clipboard fallback), and dual-path invite acceptance all functional.
</success_criteria>

<output>
After completion, create `.planning/phases/12-remaining-screens/12-03-SUMMARY.md`
</output>
