---
phase: 12-remaining-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/collections/index.tsx
  - app/(tabs)/collections/[id].tsx
  - app/(tabs)/collections/create.tsx
autonomous: true
requirements: [SCREEN-05]

must_haves:
  truths:
    - "Collection list screen shows all user collections in a responsive grid matching cookbook.pen"
    - "Collection detail screen shows collection info and recipes in a RecipeCard grid"
    - "Create collection screen renders a responsive form matching cookbook.pen"
    - "All three screens adapt correctly to mobile, tablet, and web breakpoints"
  artifacts:
    - path: "app/(tabs)/collections/index.tsx"
      provides: "Responsive collection list screen"
    - path: "app/(tabs)/collections/[id].tsx"
      provides: "Responsive collection detail with RecipeCard grid"
    - path: "app/(tabs)/collections/create.tsx"
      provides: "Responsive create collection form"
  key_links:
    - from: "app/(tabs)/collections/index.tsx"
      to: "src/features/collections/api.ts"
      via: "import getCollections"
      pattern: "getCollections"
    - from: "app/(tabs)/collections/[id].tsx"
      to: "src/components/recipes/RecipeCard.tsx"
      via: "import RecipeCard for recipe grid"
      pattern: "RecipeCard"
    - from: "app/(tabs)/collections/[id].tsx"
      to: "src/features/collections/api.ts"
      via: "import getCollectionById, getCollectionRecipes"
      pattern: "getCollection(ById|Recipes)"
---

<objective>
Rebuild collection screens (list, detail, create) to match cookbook.pen at all 3 breakpoints with responsive grids and token-based styling.

Purpose: Collections organize recipes into user-created groups. The screens need responsive layouts matching the design system.
Output: Three responsive collection screens using tokens, useBreakpoint, and RecipeCard grid on detail.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-remaining-screens/12-CONTEXT.md
@.planning/phases/12-remaining-screens/12-RESEARCH.md
@cookbook.pen
@app/(tabs)/collections/index.tsx
@app/(tabs)/collections/[id].tsx
@app/(tabs)/collections/create.tsx
@app/(tabs)/collections/_layout.tsx
@src/features/collections/api.ts
@src/features/collections/types.ts
@src/components/recipes/RecipeCard.tsx
@src/components/recipes/recipeCardUtils.ts
@src/components/nav/PageContainer.tsx
@src/lib/tokens.ts
@src/lib/hooks/useBreakpoint.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild collection list and create screens</name>
  <files>app/(tabs)/collections/index.tsx, app/(tabs)/collections/create.tsx</files>
  <action>
**Collection list (`index.tsx`):**
Rebuild to match cookbook.pen. Preserve all existing data fetching logic (getCollections from `@/features/collections/api`).

- Wrap in PageContainer
- Use useBreakpoint() for responsive layout
- Header: "Collections" title using fontFamilyDisplay, fontSize2xl, textPrimary tokens
- "New Collection" button (Pressable, accentBlue bg, white text, radiusMd) — navigates to create screen
- FlatList of collection cards in responsive grid:
  - Mobile: 1 column
  - Tablet: 2 columns
  - Web: 3 columns
  - Use `numColumns`, `key={numColumns}`, `columnWrapperStyle` with gap when numColumns > 1
  - On web: `style={{ flexGrow: 1, flexBasis: 0 }}` for FlatList
- Each collection card: name, recipe count, optional description preview
  - bgCard background, radiusMd border radius, shadowSm shadow, padding 16
  - Pressable navigating to collection detail [id]
- Empty state: "No collections yet" message with Create CTA (Claude's discretion on exact design)
- Loading state: ActivityIndicator centered

**Create collection (`create.tsx`):**
Rebuild to match cookbook.pen. Preserve existing createCollection API call.

- PageContainer wrapper
- Responsive form: full-width on mobile, maxWidth 600 centered on tablet/web
- Fields: name (required), description (optional, multiline TextInput)
- Submit button: "Create Collection" with accentBlue bg
- Cancel button or back navigation
- All text inputs: borderWidth 1, borderColor borderDefault, radiusMd, padding 12, fontFamilyBody
- Error handling: show error message below form if create fails

Do NOT use StyleSheet.create for breakpoint-dependent styles.
Do NOT use TouchableOpacity — use Pressable.
All colors from tokens.ts, all fonts from tokens.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit app/\(tabs\)/collections/index.tsx app/\(tabs\)/collections/create.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Collection list shows responsive grid of collection cards. Create form has responsive layout. Both use tokens and useBreakpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild collection detail screen with RecipeCard grid</name>
  <files>app/(tabs)/collections/[id].tsx</files>
  <action>
Rebuild collection detail to match cookbook.pen. Preserve all existing data fetching (getCollectionById, getCollectionRecipes) and management logic (deleteCollection, removeRecipeFromCollection).

- PageContainer wrapper
- Use useBreakpoint() for responsive layout
- Header section: collection name (fontFamilyDisplay, fontSize2xl), description, recipe count
- Action buttons: Edit, Delete, Add Recipe — styled with tokens (Pressable, not TouchableOpacity)
- Recipe grid using RecipeCard component:
  - Import RecipeCard from `@/components/recipes/RecipeCard`
  - Import getNumColumns from `@/components/recipes/recipeCardUtils` (or compute: mobile=1, tablet=2, web=3)
  - FlatList with numColumns, key={numColumns}, gap 16
  - On web: flexGrow: 1, flexBasis: 0 on FlatList style
- Fetch recipe thumbnail URLs using getRecipeThumbnailUrlMap (batch fetch before render, same pattern as Home screen in Phase 10)
- Each recipe card has option to remove from collection (confirm dialog)
- Empty state when collection has no recipes: "No recipes in this collection" + Add Recipe CTA
- Loading: ActivityIndicator while fetching
- Error: show error message if fetch fails

Per CONTEXT.md locked decision: collection detail uses RecipeCard grid. If RecipeCard doesn't match .pen for this context, update it — but likely it already matches from Phase 10 work.

Do NOT use StyleSheet.create for breakpoint-dependent styles.
  </action>
  <verify>
    <automated>npx tsc --noEmit app/\(tabs\)/collections/\[id\].tsx 2>&1 | head -20</automated>
  </verify>
  <done>Collection detail shows collection info header and recipes in a responsive RecipeCard grid. Remove-from-collection action works. All styling uses tokens and useBreakpoint.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all modified files
- Collection list shows responsive grid at all breakpoints
- Collection detail shows RecipeCard grid with proper column counts
- Create collection form is responsive
</verification>

<success_criteria>
All three collection screens match cookbook.pen at mobile/tablet/web breakpoints. RecipeCard grid used on detail screen. Existing CRUD functionality preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/12-remaining-screens/12-02-SUMMARY.md`
</output>
