---
phase: 11-public-browsing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/public/PublicNavHeader.tsx
  - src/components/public/PublicSearchBar.tsx
  - src/components/public/__tests__/PublicNavHeader.test.ts
  - src/components/public/AdSlot.native.tsx
  - src/components/public/AdSlot.web.tsx
autonomous: true
requirements: [PUB-03]

must_haves:
  truths:
    - "Public navigation header renders logo, Sign In, and search on all breakpoints"
    - "Web header shows Get Started CTA button alongside Sign In"
    - "Mobile browse header scrolls with content (not sticky)"
    - "Mobile/tablet detail nav bar has back arrow and Sign In"
    - "Web detail nav bar shows Sign In and Get Started buttons matching the browse header"
    - "Ad slot placeholder renders correct dimensions per breakpoint with platform branching"
  artifacts:
    - path: "src/components/public/PublicNavHeader.tsx"
      provides: "Shared public navigation header with breakpoint-responsive layout"
      exports: ["PublicBrowseHeader", "PublicDetailNavBar"]
      min_lines: 80
    - path: "src/components/public/PublicSearchBar.tsx"
      provides: "Reusable search bar component matching cookbook.pen Component/SearchBar spec"
      exports: ["PublicSearchBar"]
    - path: "src/components/public/__tests__/PublicNavHeader.test.ts"
      provides: "Unit tests for PublicNavHeader breakpoint rendering"
    - path: "src/components/public/AdSlot.native.tsx"
      provides: "Native ad slot placeholder (Phase 13 replaces with AdMob)"
      exports: ["AdSlot"]
    - path: "src/components/public/AdSlot.web.tsx"
      provides: "Web ad slot placeholder (Phase 13 replaces with web ads)"
      exports: ["AdSlot"]
  key_links:
    - from: "src/components/public/PublicNavHeader.tsx"
      to: "src/lib/hooks/useBreakpoint.ts"
      via: "useBreakpoint() for layout switching"
      pattern: "useBreakpoint"
    - from: "src/components/public/AdSlot.native.tsx"
      to: "src/components/public/AdSlot.web.tsx"
      via: "React Native platform file resolution (.native.tsx vs .web.tsx)"
      pattern: "AdSlot"
---

<objective>
Build the shared UI components for public screens: navigation header (3 breakpoints, 2 variants), search bar, and platform-branched ad slot placeholder.

Purpose: Both the public browse and detail screens need the navigation header. Building these as shared components avoids duplication and establishes the public chrome that distinguishes unauthenticated views from the authenticated app.

Output: PublicNavHeader (with browse and detail variants), PublicSearchBar, AdSlot (platform-branched placeholders), and PublicNavHeader unit tests.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-public-browsing/11-CONTEXT.md
@.planning/phases/11-public-browsing/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/tokens.ts:
```typescript
export const accentWarm = '#E8784E';
export const bgCard = '#F6F7F8';
export const bgCardWarm = '#FFFBF5';
export const borderDefault = '#E5E7EB';
export const borderSubtle = '#F3F4F6';
export const textPrimary = '#1A1A1A';
export const textSecondary = '#6B7280';
export const textTertiary = '#9CA3AF';
export const white = '#FFFFFF';
export const radiusPill = 100;
export const radiusSm = 12;
export const radiusMd = 16;
export const fontFamilyDisplay = 'BricolageGrotesque_600SemiBold';
export const fontFamilyBody = 'DMSans_400Regular';
export const fontFamilyBodyMedium = 'DMSans_500Medium';
export const fontFamilyBodyBold = 'DMSans_700Bold';
```

From src/lib/hooks/useBreakpoint.ts:
```typescript
export type Breakpoint = 'mobile' | 'tablet' | 'web';
export function useBreakpoint(): { breakpoint: Breakpoint; width: number };
```

Icons available (lucide-react-native):
- BookOpen, Search, ArrowLeft, ArrowRight, Megaphone
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PublicSearchBar and PublicNavHeader components with tests</name>
  <files>
    src/components/public/PublicSearchBar.tsx,
    src/components/public/PublicNavHeader.tsx,
    src/components/public/__tests__/PublicNavHeader.test.ts
  </files>
  <behavior>
    - PublicBrowseHeader at mobile breakpoint: renders BookOpen icon, "Cookbook" text, "Sign In" button, PublicSearchBar, and filter chips
    - PublicBrowseHeader at tablet breakpoint: renders BookOpen icon, "Cookbook" text, PublicSearchBar (width 320), "Sign In" button, and filter chips (7 total including Vegetarian and Comfort)
    - PublicBrowseHeader at web breakpoint: renders BookOpen icon, "Cookbook" text, PublicSearchBar (width 480), "Sign In" button, and "Get Started" button; does NOT render filter chips
    - PublicDetailNavBar at mobile breakpoint: renders ArrowLeft icon, "Cookbook" text, and "Sign In" button
    - PublicDetailNavBar at tablet breakpoint: renders ArrowLeft icon, "Cookbook" text, PublicSearchBar, and "Sign In" button
    - PublicDetailNavBar at web breakpoint: renders BookOpen icon (not ArrowLeft), "Cookbook" text, PublicSearchBar, "Sign In" button, and "Get Started" button
  </behavior>
  <action>
    **1. Create `src/components/public/PublicSearchBar.tsx`:**

    A reusable search bar matching cookbook.pen `Component/SearchBar` spec:
    - Props: `{ value: string; onChangeText: (text: string) => void; placeholder?: string; style?: object }`
    - Container: height 48, bg `bgCard`, borderRadius 26, paddingHorizontal 18, gap 12, flexDirection 'row', alignItems 'center'
    - Search icon: lucide `Search`, size 18, color `textTertiary`
    - TextInput: flex 1, fontSize 15, fontFamily `fontFamilyBody`, color `textPrimary`, placeholderTextColor `textTertiary`, placeholder defaults to "Search recipes..."
    - Outer `style` prop merges with container style (for width overrides like 320px on tablet, 480px on web)

    **2. Create `src/components/public/PublicNavHeader.tsx`:**

    Export TWO components from the same file:

    **`PublicBrowseHeader`** — used on the browse screen, scrolls with content (NOT sticky):
    - Props: `{ searchQuery: string; onSearchChange: (q: string) => void; selectedTag: string; onTagChange: (tag: string) => void }`
    - Uses `useBreakpoint()` internally for layout switching
    - Uses `useSafeAreaInsets()` for top padding on mobile/tablet

    Mobile layout (vertical stack):
    - Background: `bgCardWarm`, paddingHorizontal 24, paddingTop `insets.top + 16`, paddingBottom 16, gap 16
    - No bottom border
    - Top row: flexDirection 'row', justifyContent 'space-between', alignItems 'center'
      - Left: BookOpen icon (24x24, accentWarm) + "Cookbook" text (fontSize 20, fontFamily 'BricolageGrotesque_700Bold' — use the 700 weight directly, it is loaded in _layout.tsx as BricolageGrotesque_700Bold, so fontFamily: 'BricolageGrotesque_700Bold'), gap 8, alignItems 'center'
      - Right: Sign In button — height 48, bg `bgCard`, borderRadius `radiusPill`, borderWidth 1, borderColor `borderDefault`, paddingHorizontal 24, paddingVertical 12. Text: "Sign In", fontSize 15, fontFamily `fontFamilyBodyMedium`, color `textPrimary`. onPress: `router.push('/(auth)/login')`
    - Search bar: `<PublicSearchBar>` full width
    - Filter chips: horizontal ScrollView, gap 8
      - Tags: ['All', 'Dinner', 'Baking', 'Dessert', 'Quick']
      - Active chip: bg `accentWarm`, borderRadius `radiusPill`, paddingHorizontal 14, paddingVertical 6, text fontSize 12, fontFamily `fontFamilyBodyMedium`, color `white`
      - Inactive chip: bg `bgCard`, same borderRadius/padding, text fontSize 12, fontFamily `fontFamilyBodyMedium`, color `textSecondary`

    Tablet layout (vertical stack):
    - Background: `bgCardWarm`, paddingHorizontal 32, paddingTop `insets.top + 20`, paddingBottom 20, gap 16
    - No bottom border
    - Top row: justifyContent 'space-between', alignItems 'center'
      - Left: BookOpen 28x28 accentWarm + "Cookbook" fontSize 22 BricolageGrotesque_700Bold
      - Right: row with gap 12 — PublicSearchBar (width 320) + Sign In button (same style as mobile)
    - Filter chips: same as mobile plus 'Vegetarian' and 'Comfort' (7 total)

    Web layout (single horizontal row):
    - Background: `bgCardWarm`, paddingHorizontal 48, paddingVertical 16, flexDirection 'row', justifyContent 'space-between', alignItems 'center'
    - No bottom border
    - Left: BookOpen 28x28 accentWarm + "Cookbook" fontSize 22 BricolageGrotesque_700Bold, gap 10
    - Center: PublicSearchBar (width 480)
    - Right: row with gap 12, alignItems 'center'
      - Sign In button (same Secondary style)
      - Get Started button: bg `accentWarm`, borderRadius `radiusPill`, height 48, paddingHorizontal 24, paddingVertical 12, flexDirection 'row', gap 8, alignItems 'center'. Text: "Get Started", fontSize 15, fontFamily `fontFamilyBodyMedium`, color `white`. ArrowRight icon 16x16 white. onPress: `router.push('/(auth)/signup')`

    NOTE on filter chips for web: They go in the browse body area (below the header), NOT in the header itself. The web header is a single horizontal row. Filter chips on web are rendered by the browse screen directly. Only mobile and tablet headers include chips.

    **`PublicDetailNavBar`** — used on the detail screen, sticky above ScrollView:
    - Props: `{ onBack: () => void }`
    - Uses `useBreakpoint()` internally
    - Uses `useSafeAreaInsets()` for top padding

    Mobile layout:
    - paddingHorizontal 20, paddingVertical 10, paddingTop `insets.top + 10`, flexDirection 'row', justifyContent 'space-between', alignItems 'center', borderBottomWidth 1, borderBottomColor `borderSubtle`, backgroundColor `white`
    - Left: Pressable with ArrowLeft 20x20 `textSecondary` + "Cookbook" fontSize 18 BricolageGrotesque_700Bold `textPrimary`, gap 8, alignItems 'center'. onPress: onBack
    - Right: Sign In button (same Secondary style)

    Tablet layout:
    - Same structure, paddingHorizontal 32, paddingVertical 12
    - Left: ArrowLeft 20x20 + "Cookbook" fontSize 20, gap 10
    - Right: row gap 12 — PublicSearchBar (width 260) + Sign In button

    Web layout:
    - paddingHorizontal 48, paddingVertical 12, flexDirection 'row', justifyContent 'space-between', alignItems 'center', borderBottomWidth 1, borderBottomColor `borderSubtle`, backgroundColor `white`
    - Left: BookOpen 28x28 + "Cookbook" fontSize 22, gap 10 (logo, not back arrow)
    - Center: PublicSearchBar (width 480)
    - Right: Sign In + Get Started (same as browse header web)

    **3. Create `src/components/public/__tests__/PublicNavHeader.test.ts`:**

    Test the breakpoint-specific rendering behavior of both components. Use the same test pattern as existing component tests (mock react-native, mock useBreakpoint, mock useSafeAreaInsets, mock expo-router). Extract testable logic into pure helper functions where possible (e.g., a `getChipsForBreakpoint(breakpoint)` helper that returns the tag array).

    If full React rendering is not feasible in the node test environment (same constraint as Phase 9), test pure logic extraction:
    - `getChipsForBreakpoint('mobile')` returns 5 chips, `getChipsForBreakpoint('tablet')` returns 7, `getChipsForBreakpoint('web')` returns 0 (no chips in web header)
    - `getHeaderLayout(breakpoint, variant)` returns expected configuration per breakpoint/variant
    - Verify that web layouts include "Get Started" CTA in both browse and detail variants
    - Verify that mobile/tablet detail nav bars use ArrowLeft, while web uses BookOpen

    Export these pure helpers alongside the components for testability.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=PublicNavHeader --no-coverage</automated>
  </verify>
  <done>PublicSearchBar, PublicBrowseHeader, and PublicDetailNavBar are exported from their respective files. All three breakpoint layouts implemented per cookbook.pen spec. Sign In routes to /(auth)/login, Get Started routes to /(auth)/signup. Pure helper tests pass validating breakpoint-specific logic. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create platform-branched AdSlot placeholder component</name>
  <files>
    src/components/public/AdSlot.native.tsx,
    src/components/public/AdSlot.web.tsx
  </files>
  <action>
    Per STATE.md requirement: "AdSlot must be platform-branched (AdSlot.native.tsx / AdSlot.web.tsx) from the start — AdMob SDK breaks web build if imported directly."

    Both files export the same component with the same props interface. Phase 11 renders a static placeholder on BOTH platforms. Phase 13 will replace the native file with real AdMob integration.

    **Props interface (identical in both files):**
    ```typescript
    type AdSlotProps = {
      variant: 'mobile' | 'leaderboard';
      style?: object;
    };
    ```

    **`src/components/public/AdSlot.native.tsx`:**
    - Export `AdSlot` as default function component
    - `mobile` variant: width 320, height 50, bg '#F9FAFB', borderRadius 8, borderWidth 1, borderColor `borderDefault`, alignItems 'center', justifyContent 'center', flexDirection 'row', gap 6
      - Megaphone icon: size 14, color `textTertiary`
      - Text "Sponsored": fontSize 10, fontFamily `fontFamilyBodyMedium`, color `textTertiary`
    - `leaderboard` variant: width 728, height 90, same bg/border style, borderRadius 10
      - Megaphone icon: size 16, color `textTertiary`
      - Text "Sponsored": fontSize 11, fontFamily `fontFamilyBodyMedium`, color `textTertiary`
    - Outer style prop merges

    **`src/components/public/AdSlot.web.tsx`:**
    - Identical implementation to native (both are placeholders in Phase 11)
    - Same props, same output — the file split is structural for Phase 13

    Consumers import as: `import AdSlot from '@/components/public/AdSlot'`
    React Native's platform resolution picks .native.tsx or .web.tsx automatically.

    NOTE: Do NOT create an `AdSlot.tsx` barrel file. The `.native.tsx` / `.web.tsx` convention is handled by the bundler. An `AdSlot.tsx` would shadow both platform files.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>AdSlot.native.tsx and AdSlot.web.tsx both exist with identical placeholder implementations. Import via '@/components/public/AdSlot' resolves per platform. Mobile variant renders 320x50, leaderboard renders 728x90. Both show Megaphone icon + "Sponsored" label. TypeScript compiles.</done>
</task>

</tasks>

<verification>
npx jest --testPathPattern=PublicNavHeader --no-coverage && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30
</verification>

<success_criteria>
- PublicBrowseHeader renders correctly at mobile (vertical with chips), tablet (vertical with search + chips), and web (horizontal row, no chips)
- PublicDetailNavBar renders back arrow on mobile/tablet, logo on web, Sign In on all, Get Started on web
- PublicSearchBar is reusable with configurable width
- AdSlot is platform-branched with .native.tsx and .web.tsx files
- PublicNavHeader tests pass validating breakpoint-specific logic
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-public-browsing/11-02-SUMMARY.md`
</output>
