---
phase: 10-core-screens
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(tabs)/recipes/[id].tsx
  - app/(tabs)/recipes/[id]/cook.tsx
autonomous: false
gap_closure: true
requirements: [SCREEN-03, SCREEN-04a]

must_haves:
  truths:
    - "Recipe detail sticky header renders fully below the iOS status bar / Dynamic Island"
    - "Start Cooking button in the sticky header is visible and tappable"
    - "Cooking mode top bar (X button, recipe title) renders fully below the iOS status bar / Dynamic Island"
    - "All screens in the recipes Stack respect safe area boundaries"
  artifacts:
    - path: "app/(tabs)/recipes/[id].tsx"
      provides: "Recipe detail with safe area inset on sticky header"
      contains: "useSafeAreaInsets"
    - path: "app/(tabs)/recipes/[id]/cook.tsx"
      provides: "Cooking mode with safe area inset on top bar"
      contains: "useSafeAreaInsets"
  key_links:
    - from: "app/(tabs)/recipes/[id].tsx"
      to: "react-native-safe-area-context"
      via: "useSafeAreaInsets import"
      pattern: "useSafeAreaInsets"
    - from: "app/(tabs)/recipes/[id]/cook.tsx"
      to: "react-native-safe-area-context"
      via: "useSafeAreaInsets import"
      pattern: "useSafeAreaInsets"
---

<objective>
Fix safe area inset handling on recipe detail and cooking mode screens so sticky headers render below the iOS status bar / Dynamic Island.

Purpose: UAT tests 6, 8, 9, 10, and 14 are blocked or failed because the recipe detail sticky header renders behind the iOS status bar. The Start Cooking button is untappable, which cascades to block all cooking mode testing. Both screens in the recipes Stack use raw Views with no safe area padding while the Stack layout has headerShown:false.

Output: Both screens import useSafeAreaInsets and apply paddingTop:insets.top to their top-level header Views, matching the established codebase pattern (PageContainer, profile screen).
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-screens/10-02-SUMMARY.md
@.planning/debug/recipe-detail-safe-area.md

<interfaces>
<!-- Established safe area pattern from PageContainer -->

From src/components/nav/PageContainer.tsx:
```typescript
import { useSafeAreaInsets } from 'react-native-safe-area-context';
// ...
const insets = useSafeAreaInsets();
// Applied as: paddingTop: insets.top on the outer View
```

From app/(tabs)/recipes/_layout.tsx:
```typescript
// Stack with headerShown: false — each screen must handle its own safe area
<Stack screenOptions={{ headerShown: false }} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safe area insets to recipe detail and cooking mode headers</name>
  <files>app/(tabs)/recipes/[id].tsx, app/(tabs)/recipes/[id]/cook.tsx</files>
  <action>
Fix both screens to respect iOS safe area boundaries. The root cause is documented in `.planning/debug/recipe-detail-safe-area.md`.

**app/(tabs)/recipes/[id].tsx:**
1. Add `useSafeAreaInsets` to the import from `react-native-safe-area-context`
2. Inside the component function (after hooks, before render), add: `const insets = useSafeAreaInsets();`
3. On the sticky header View (the first child View inside the outer `<View style={{ flex: 1, backgroundColor: bgPage }}>`, around line 1028), add `paddingTop: insets.top` to its style object. Keep the existing paddingVertical:12 — insets.top is additive safe area offset, paddingVertical is content spacing. Change paddingVertical to paddingBottom only (12) so that the top padding comes entirely from insets.top (which is sufficient on its own for status bar clearance — adding paddingVertical on top would create excessive spacing).
4. Also apply `paddingTop: insets.top` to the early-return loading/error/not-found Views so those states also clear the status bar.

**app/(tabs)/recipes/[id]/cook.tsx:**
1. Add `useSafeAreaInsets` to imports from `react-native-safe-area-context`
2. Inside the component function, add: `const insets = useSafeAreaInsets();`
3. On the top bar View (around line 235, the View with `flexDirection: 'row', alignItems: 'center', padding: 16`), add `paddingTop: Math.max(insets.top, 16)` and keep the rest of the padding as `paddingBottom: 16, paddingHorizontal: 16` (replace the shorthand `padding: 16`).
4. Also apply `paddingTop: insets.top` to the early-return loading/error/no-steps Views so those states also clear the status bar.

Do NOT wrap these screens in PageContainer — they have custom layouts (sticky header above ScrollView, cooking mode step navigation) that are incompatible with PageContainer's wrapper.

The outer `<View style={{ flex: 1, backgroundColor: bgPage }}>` should NOT get paddingTop — keep it at flex:1 only so the background color extends behind the status bar for visual continuity. Only the first interactive child (the header) gets the inset padding.
  </action>
  <verify>
    <automated>cd /Users/elinicholson/development/cookbook && npx tsc --noEmit 2>&1 | tail -5 && grep -c "useSafeAreaInsets" app/\(tabs\)/recipes/\[id\].tsx app/\(tabs\)/recipes/\[id\]/cook.tsx && grep -c "insets.top" app/\(tabs\)/recipes/\[id\].tsx app/\(tabs\)/recipes/\[id\]/cook.tsx</automated>
  </verify>
  <done>Both files import useSafeAreaInsets, both apply insets.top to their header Views, TypeScript compiles cleanly, and grep confirms the pattern is present in both files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify safe area fix on iOS device</name>
  <action>
User verifies on a physical iOS device that the recipe detail sticky header and cooking mode top bar render below the status bar / Dynamic Island.
  </action>
  <verify>User confirms visual correctness</verify>
  <done>Recipe detail and cooking mode headers are fully visible below the iOS status bar</done>
  <what-built>Safe area inset padding added to recipe detail sticky header and cooking mode top bar. Both screens should now render their header UI fully below the iOS status bar / Dynamic Island.</what-built>
  <how-to-verify>
    1. Open the Expo dev client on your iOS device
    2. Navigate to any recipe detail screen
    3. Verify: The sticky header (back arrow + Start Cooking button) renders fully below the status bar / Dynamic Island — no overlap
    4. Tap "Start Cooking" — it should be tappable and navigate to cooking mode
    5. In cooking mode, verify: The top bar (X button + recipe title + step counter) renders fully below the status bar / Dynamic Island — no overlap
    6. Navigate through cooking steps using Next/Previous to confirm the rest of the screen works normally
    7. Tap X to exit back to recipe detail — confirm navigation works
  </how-to-verify>
  <resume-signal>Type "approved" if all screens respect safe area boundaries, or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors: `npx tsc --noEmit`
- Both files contain `useSafeAreaInsets` import and `insets.top` usage
- Visual verification on iOS device confirms headers clear the status bar
- Start Cooking button is tappable (unblocks UAT tests 8, 9, 10)
</verification>

<success_criteria>
- UAT test 6 (recipe detail safe area): PASSES — sticky header below status bar
- UAT test 8 (Start Cooking navigation): UNBLOCKED and PASSES — button visible and tappable
- UAT test 9 (cooking mode step display): UNBLOCKED — top bar below status bar
- UAT test 10 (cooking mode navigation): UNBLOCKED — screen fully functional
- UAT test 14 (all screens safe area): PASSES for recipe detail and cooking mode
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-screens/10-07-SUMMARY.md`
</output>
