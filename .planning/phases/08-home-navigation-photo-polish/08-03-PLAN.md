---
phase: 08-home-navigation-photo-polish
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - cookbook.pen
autonomous: false
requirements: [DESIGN-04]

must_haves:
  truths:
    - "cookbook.pen contains a Sign Up screen design at all 3 breakpoints (mobile, tablet, web)"
    - "cookbook.pen contains a Forgot Password screen design at all 3 breakpoints"
    - "cookbook.pen contains a Profile/Settings screen design at all 3 breakpoints"
    - "cookbook.pen contains an Invite screen design at all 3 breakpoints"
    - "cookbook.pen contains a Draft Review screen design at all 3 breakpoints"
  artifacts:
    - path: "cookbook.pen"
      provides: "5 new screen designs x 3 breakpoints = 15 new screen layouts"
      contains: "Sign Up, Forgot Password, Profile/Settings, Invite, Draft Review artboards"
  key_links:
    - from: "cookbook.pen new screen designs"
      to: "Phase 12 implementation"
      via: "design spec reference"
      pattern: "screen designs feed directly into Phase 12 (Remaining Screens)"
---

<objective>
Create the 5 missing screen designs in cookbook.pen so Phase 12 has complete design specs to implement against.

Purpose: Phase 12 (Remaining Screens) cannot rebuild auth, profile, invite, or draft review screens without designs at all 3 breakpoints. These designs must exist before Phase 12 begins.
Output: cookbook.pen updated with Sign Up, Forgot Password, Profile/Settings, Invite, and Draft Review screen designs at mobile (390px), tablet (768px), and web (1440px) breakpoints.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-home-navigation-photo-polish/08-CONTEXT.md
@cookbook.pen (existing design file — contains Login screen and other existing designs as reference)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create 5 missing screen designs in cookbook.pen</name>
  <files>cookbook.pen</files>
  <action>
    Create 5 new screen designs in cookbook.pen. Each screen needs layouts at 3 breakpoints (mobile 390px, tablet 768px, web 1440px) for a total of 15 new artboards.

    **Screen design specifications (from user decisions in CONTEXT.md):**

    1. **Sign Up** -- Follow the same layout pattern as the existing Login screen:
       - Mobile: Full-screen form (name, email, password, confirm password, submit button)
       - Tablet: Centered card on gray background
       - Web: Split layout -- hero image/illustration left, form right
       - Use the same spacing, typography, and component sizes as Login
       - Different fields and copy, same structural bones

    2. **Forgot Password** -- Follow the same layout pattern as the existing Login screen:
       - Mobile: Full-screen form (email field, submit button, "Back to Login" link)
       - Tablet: Centered card on gray background
       - Web: Split layout -- hero left, form right
       - Same structural bones as Login, different content

    3. **Profile/Settings** -- Single scrollable page, NOT sectioned:
       - Avatar (circle, editable) at top
       - Display name field
       - Email field (read-only or with change flow)
       - Unit preference toggle (metric/imperial)
       - Logout button at bottom
       - All on one scrollable page, no tabs or sections

    4. **Invite** -- Link sharing as primary interaction:
       - Generate/copy invite link as the primary CTA
       - Optional email entry field below the link section
       - Keep it simple -- one clear primary action

    5. **Draft Review** -- Photo + extracted fields:
       - Mobile: Collapsible photo -- photo shown initially, collapses to thumbnail as user scrolls into the extracted form fields. Keeps scan reference accessible without eating screen space.
       - Tablet: Side-by-side -- photo on the left, extracted fields on the right
       - Web: Side-by-side -- photo on the left, extracted fields on the right (wider layout)

    **Design tokens to use:** Reference the existing cookbook.pen $ variables for colors, fonts, and radii. The tokens from Plan 01 (accentWarm, bgPage, textPrimary, etc.) map directly to these variables.

    **Also resolve (from STATE.md watch item):** The tablet nav pattern (768px) is flagged as ambiguous in cookbook.pen. While creating these 5 screens at the tablet breakpoint, ensure the tablet header navigation is clear and consistent across all screens. This unblocks Phase 9's TabletHeader implementation.
  </action>
  <verify>
    Open cookbook.pen and confirm:
    1. Sign Up screen exists at mobile (390px), tablet (768px), and web (1440px) breakpoints
    2. Forgot Password screen exists at all 3 breakpoints
    3. Profile/Settings screen exists at all 3 breakpoints
    4. Invite screen exists at all 3 breakpoints
    5. Draft Review screen exists at all 3 breakpoints -- mobile shows collapsible photo pattern, tablet/web show side-by-side
    6. All screens use existing cookbook.pen $ variables consistently
    7. Tablet navigation pattern is clear and unambiguous across all screens

    Type "designs complete" when all 15 artboards exist in cookbook.pen, or describe what is missing.
  </verify>
  <done>
    - 15 new artboards exist in cookbook.pen (5 screens x 3 breakpoints)
    - Sign Up and Forgot Password follow the existing Login screen layout pattern
    - Profile/Settings is a single scrollable page with avatar, name, email, unit pref, logout
    - Invite has link sharing as primary interaction
    - Draft Review has collapsible photo on mobile, side-by-side on tablet/web
    - Tablet navigation pattern is resolved and unambiguous
  </done>
</task>

</tasks>

<verification>
Human visual inspection of cookbook.pen -- all 5 screens x 3 breakpoints = 15 artboards present and consistent with existing design language.
</verification>

<success_criteria>
- cookbook.pen contains Sign Up, Forgot Password, Profile/Settings, Invite, and Draft Review designs
- Each screen has mobile (390px), tablet (768px), and web (1440px) layouts
- Sign Up and Forgot Password follow the existing Login screen layout pattern
- Profile/Settings is a single scrollable page with avatar, name, email, unit pref, logout
- Invite has link sharing as primary interaction
- Draft Review has collapsible photo on mobile, side-by-side on tablet/web
- Tablet navigation pattern is resolved and unambiguous
</success_criteria>

<output>
After completion, create `.planning/phases/08-home-navigation-photo-polish/08-03-SUMMARY.md`
</output>
