---
phase: 08-home-navigation-photo-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tokens.ts
  - src/lib/__tests__/tokens.test.ts
  - src/lib/hooks/useBreakpoint.ts
  - src/lib/hooks/__tests__/useBreakpoint.test.ts
autonomous: true
requirements: [DESIGN-01, DESIGN-02]

must_haves:
  truths:
    - "Any screen can import design tokens from @/lib/tokens and use them in styles"
    - "useBreakpoint() returns 'mobile' for widths under 640px"
    - "useBreakpoint() returns 'tablet' for widths 640-1279px"
    - "useBreakpoint() returns 'web' for widths 1280px and above"
    - "All 24 cookbook.pen $ variables are represented as TypeScript constants"
    - "Font size scale (xs through 3xl) and shadow tokens (sm, md, lg) exist"
  artifacts:
    - path: "src/lib/tokens.ts"
      provides: "All design token constants"
      exports: ["colorBrand (accentWarm)", "colorBackground (bgPage)", "all 24 pen vars", "fontSizeXs through fontSize3xl", "shadowSm/shadowMd/shadowLg", "fontFamilyDisplay/fontFamilyBody"]
    - path: "src/lib/hooks/useBreakpoint.ts"
      provides: "Responsive breakpoint detection"
      exports: ["useBreakpoint", "getBreakpoint", "Breakpoint", "BreakpointResult"]
    - path: "src/lib/__tests__/tokens.test.ts"
      provides: "Token export validation tests"
    - path: "src/lib/hooks/__tests__/useBreakpoint.test.ts"
      provides: "Breakpoint logic unit tests"
  key_links:
    - from: "src/lib/tokens.ts"
      to: "cookbook.pen variables section"
      via: "manual extraction"
      pattern: "exact hex values from cookbook.pen"
    - from: "src/lib/hooks/useBreakpoint.ts"
      to: "react-native useWindowDimensions"
      via: "hook composition"
      pattern: "useWindowDimensions.*width"
---

<objective>
Create the design token system and responsive breakpoint hook that every subsequent phase will import.

Purpose: Phases 9-13 all depend on consistent design tokens and breakpoint detection. These primitives must exist first.
Output: `src/lib/tokens.ts` with all cookbook.pen design variables as TypeScript constants, and `src/lib/hooks/useBreakpoint.ts` returning the current breakpoint. Both fully tested.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-home-navigation-photo-polish/08-CONTEXT.md
@.planning/phases/08-home-navigation-photo-polish/08-RESEARCH.md
@cookbook.pen (read lines 16585-16682 for the variables section)
@jest.config.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Design tokens with all cookbook.pen variables</name>
  <files>src/lib/tokens.ts, src/lib/__tests__/tokens.test.ts</files>
  <behavior>
    - Test: All 24 cookbook.pen $ variables are exported as named constants
    - Test: Color tokens are strings starting with '#'
    - Test: Radius tokens are numbers
    - Test: Font family tokens are non-empty strings
    - Test: Font size scale exports exist (xs, sm, base, lg, xl, 2xl, 3xl) as numbers
    - Test: Shadow tokens (sm, md, lg) are objects with shadowColor, shadowOffset, shadowOpacity, shadowRadius, elevation
    - Test: No duplicate token values where they shouldn't be (each color is distinct)
  </behavior>
  <action>
    **RED phase:** Create `src/lib/__tests__/tokens.test.ts` first. Write tests that import from `@/lib/tokens` and assert:
    1. All 24 cookbook.pen variables exist as exports using flat-with-category-prefix naming
    2. Color values match cookbook.pen exactly (see mapping below)
    3. Font size scale has 7 entries (xs=12, sm=14, base=16, lg=18, xl=20, 2xl=24, 3xl=30)
    4. Shadow tokens have required RN shadow properties
    Run tests — they MUST fail.

    **GREEN phase:** Create `src/lib/tokens.ts` with flat-with-category-prefix naming. Extract ALL 24 variables from cookbook.pen verbatim:

    cookbook.pen variable -> token name mapping:
    - accent-blue (#007AFF) -> accentBlue
    - accent-coral (#FF6B6B) -> accentCoral
    - accent-green (#22C55E) -> accentGreen
    - accent-warm (#E8784E) -> accentWarm
    - accent-yellow (#FCD34D) -> accentYellow
    - badge-coral-bg (#FFF1F0) -> badgeCoralBg
    - badge-green-bg (#F0FDF4) -> badgeGreenBg
    - badge-yellow-bg (#FFFBEB) -> badgeYellowBg
    - bg-card (#F6F7F8) -> bgCard
    - bg-card-warm (#FFFBF5) -> bgCardWarm
    - bg-page (#FFFFFF) -> bgPage
    - border-default (#E5E7EB) -> borderDefault
    - border-subtle (#F3F4F6) -> borderSubtle
    - font-body ("DM Sans") -> fontBody (raw string, not the RN family key)
    - font-display ("Bricolage Grotesque") -> fontDisplay (raw string)
    - radius-lg (20) -> radiusLg
    - radius-md (16) -> radiusMd
    - radius-pill (100) -> radiusPill
    - radius-sm (12) -> radiusSm
    - text-disabled (#D1D5DB) -> textDisabled
    - text-primary (#1A1A1A) -> textPrimary
    - text-secondary (#6B7280) -> textSecondary
    - text-tertiary (#9CA3AF) -> textTertiary
    - white (#FFFFFF) -> white

    Additionally add per user decision:
    - Font family constants for RN fontFamily strings: fontFamilyDisplay = 'BricolageGrotesque_600SemiBold', fontFamilyBody = 'DMSans_400Regular', fontFamilyBodyMedium = 'DMSans_500Medium', fontFamilyBodyBold = 'DMSans_700Bold'
    - Font size scale: fontSizeXs=12, fontSizeSm=14, fontSizeBase=16, fontSizeLg=18, fontSizeXl=20, fontSize2xl=24, fontSize3xl=30
    - Shadow tokens (sm, md, lg) as objects with shadowColor, shadowOffset, shadowOpacity, shadowRadius, elevation — use values from RESEARCH.md

    No spacing scale per user decision.
    Run tests — they MUST pass.

    **REFACTOR:** Review for any improvements. No structural changes needed — this is a flat constants file.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="tokens" --no-coverage</automated>
  </verify>
  <done>
    - tokens.ts exports all 24 cookbook.pen variables with exact values
    - Font size scale (7 sizes) and shadow tokens (3 levels) exported
    - Font family RN string constants exported
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Breakpoint detection hook</name>
  <files>src/lib/hooks/useBreakpoint.ts, src/lib/hooks/__tests__/useBreakpoint.test.ts</files>
  <behavior>
    - Test: getBreakpoint(390) returns 'mobile'
    - Test: getBreakpoint(639) returns 'mobile'
    - Test: getBreakpoint(640) returns 'tablet'
    - Test: getBreakpoint(768) returns 'tablet'
    - Test: getBreakpoint(1279) returns 'tablet'
    - Test: getBreakpoint(1280) returns 'web'
    - Test: getBreakpoint(1440) returns 'web'
    - Test: getBreakpoint(0) returns 'mobile' (edge case)
  </behavior>
  <action>
    **RED phase:** Create `src/lib/hooks/__tests__/useBreakpoint.test.ts`. Import `getBreakpoint` (pure function) and `Breakpoint` type from `@/lib/hooks/useBreakpoint`. Write tests for all boundary conditions listed in behavior. Run tests — they MUST fail.

    Per RESEARCH.md recommendation, extract the breakpoint-mapping logic into a pure function `getBreakpoint(width: number): Breakpoint` to make unit testing trivial without needing a React renderer. The hook itself simply calls `useWindowDimensions` and passes width to the pure function.

    **GREEN phase:** Create `src/lib/hooks/useBreakpoint.ts`:
    1. Export `type Breakpoint = 'mobile' | 'tablet' | 'web'`
    2. Export `interface BreakpointResult { breakpoint: Breakpoint; width: number }`
    3. Export `function getBreakpoint(width: number): Breakpoint` — pure function with thresholds: <640 = mobile, 640-1279 = tablet, 1280+ = web (per user decision from CONTEXT.md)
    4. Export `function useBreakpoint(): BreakpointResult` — calls `useWindowDimensions()` from react-native, passes width to `getBreakpoint`, returns `{ breakpoint, width }`

    CRITICAL CONSTRAINT from STATE.md: "All dimension-sensitive styles must be computed inside components from useBreakpoint() -- NOT cached in StyleSheet.create." The hook must use `useWindowDimensions` (NOT `Dimensions.get`), which re-renders on web window resize.

    Run tests — they MUST pass.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="useBreakpoint" --no-coverage</automated>
  </verify>
  <done>
    - getBreakpoint returns correct breakpoint for all threshold boundaries
    - useBreakpoint hook wraps getBreakpoint with useWindowDimensions
    - Breakpoint and BreakpointResult types exported
    - All tests pass
  </done>
</task>

</tasks>

<verification>
npx jest --testPathPattern="tokens|useBreakpoint" --no-coverage
npx tsc -p tsconfig.json --noEmit (no new type errors introduced)
</verification>

<success_criteria>
- `src/lib/tokens.ts` importable from any file via `@/lib/tokens`
- All 24 cookbook.pen $ variables present with exact hex/number values
- Font size scale and shadow tokens present
- `useBreakpoint()` returns correct breakpoint at all thresholds
- Pure `getBreakpoint()` function testable without React
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/08-home-navigation-photo-polish/08-01-SUMMARY.md`
</output>
