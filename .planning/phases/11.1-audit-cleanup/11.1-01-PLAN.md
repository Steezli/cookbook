---
phase: 11.1-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tokens.ts
  - app/(tabs)/index.tsx
  - src/components/recipes/RecipeCard.tsx
  - app/(public)/recipe/[id].tsx
  - app/(public)/index.tsx
  - app/(tabs)/recipes/[id].tsx
  - src/components/public/PublicNavHeader.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Zero hardcoded hex color values (#FFFFFF, #E8E0D8, #8B7355) in Phase 10/11 screen files"
    - "A fontFamilyDisplayBold token exists in tokens.ts"
    - "All raw BricolageGrotesque_700Bold strings replaced with fontFamilyDisplayBold import"
    - "noPhotoBg and noPhotoIcon tokens exist and are used everywhere the placeholder colors appeared"
  artifacts:
    - path: "src/lib/tokens.ts"
      provides: "fontFamilyDisplayBold, noPhotoBg, noPhotoIcon token exports"
      contains: "fontFamilyDisplayBold|noPhotoBg|noPhotoIcon"
  key_links:
    - from: "src/components/recipes/RecipeCard.tsx"
      to: "src/lib/tokens.ts"
      via: "import { noPhotoBg, noPhotoIcon }"
      pattern: "import.*noPhotoBg.*tokens"
    - from: "app/(public)/recipe/[id].tsx"
      to: "src/lib/tokens.ts"
      via: "import { noPhotoBg, noPhotoIcon, fontFamilyDisplayBold }"
      pattern: "import.*fontFamilyDisplayBold.*tokens"
    - from: "src/components/public/PublicNavHeader.tsx"
      to: "src/lib/tokens.ts"
      via: "import { fontFamilyDisplayBold }"
      pattern: "import.*fontFamilyDisplayBold.*tokens"
---

<objective>
Extract all hardcoded hex colors and raw font family strings into design tokens, then update every consumer file to import from tokens.ts.

Purpose: Eliminate tech debt flagged in v1.1 milestone audit -- all design values must flow through the token system established in Phase 8.
Output: Three new token exports (fontFamilyDisplayBold, noPhotoBg, noPhotoIcon) and seven consumer files updated to use them.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-audit-cleanup/11.1-RESEARCH.md

<interfaces>
<!-- Current token exports in src/lib/tokens.ts that consumer files already import -->
From src/lib/tokens.ts:
```typescript
export const fontFamilyDisplay = 'BricolageGrotesque_600SemiBold';
export const fontFamilyBody = 'DMSans_400Regular';
export const fontFamilyBodyMedium = 'DMSans_500Medium';
export const fontFamilyBodyBold = 'DMSans_700Bold';
export const white = '#FFFFFF';
// ... other color tokens already exist
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing tokens to tokens.ts</name>
  <files>src/lib/tokens.ts</files>
  <action>
Add three new token exports to src/lib/tokens.ts:

1. In the "Font family constants" section (after fontFamilyBodyBold on line 64), add:
   `export const fontFamilyDisplayBold = 'BricolageGrotesque_700Bold';`

2. After the "Background colors" section (after line 26), add a new section:
   ```
   // ---------------------------------------------------------------------------
   // Placeholder colors (no-photo states)
   // ---------------------------------------------------------------------------
   export const noPhotoBg = '#E8E0D8';
   export const noPhotoIcon = '#8B7355';
   ```

Keep existing exports unchanged.
  </action>
  <verify>
    <automated>grep -c 'fontFamilyDisplayBold\|noPhotoBg\|noPhotoIcon' src/lib/tokens.ts | grep -q '3' && echo "PASS: 3 new tokens found" || echo "FAIL"</automated>
  </verify>
  <done>tokens.ts exports fontFamilyDisplayBold, noPhotoBg, and noPhotoIcon</done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded colors and font strings in consumer files</name>
  <files>app/(tabs)/index.tsx, src/components/recipes/RecipeCard.tsx, app/(public)/recipe/[id].tsx, app/(public)/index.tsx, app/(tabs)/recipes/[id].tsx, src/components/public/PublicNavHeader.tsx</files>
  <action>
Update each file to import from tokens and remove hardcoded values. For each file:

**app/(tabs)/index.tsx:**
- Line 186: Replace `color: '#FFFFFF'` with `color: white`
- Add `white` to the existing tokens import if not already present

**src/components/recipes/RecipeCard.tsx:**
- Line 67: Replace `backgroundColor: '#E8E0D8'` with `backgroundColor: noPhotoBg`
- Line 72: Replace `color="#8B7355"` with `color={noPhotoIcon}`
- Add `noPhotoBg, noPhotoIcon` to the existing tokens import

**app/(public)/recipe/[id].tsx:**
- Line 39: Remove the local `const noPhotoBg = '#E8E0D8'` declaration
- Line 168: Replace `color="#8B7355"` with `color={noPhotoIcon}`
- Lines 328, 359, 415, 490, 546: Replace all `fontFamily: 'BricolageGrotesque_700Bold'` with `fontFamily: fontFamilyDisplayBold`
- Add `noPhotoBg, noPhotoIcon, fontFamilyDisplayBold` to the existing tokens import

**app/(public)/index.tsx:**
- Lines 52-53: Remove local `noPhotoBg` and `noPhotoIcon` const declarations
- Add `noPhotoBg, noPhotoIcon` to the existing tokens import

**app/(tabs)/recipes/[id].tsx:**
- Line 71: Remove the local `const noPhotoBg = '#E8E0D8'` declaration
- Add `noPhotoBg` to the existing tokens import

**src/components/public/PublicNavHeader.tsx:**
- Lines 102, 344, 386: Replace all `fontFamily: 'BricolageGrotesque_700Bold'` with `fontFamily: fontFamilyDisplayBold`
- Add `fontFamilyDisplayBold` to the existing tokens import

IMPORTANT: When removing local const declarations (noPhotoBg in recipe/[id].tsx, index.tsx, recipes/[id].tsx), make sure the token import is added in the same edit to avoid redeclaration errors. Line numbers are from research and may have shifted slightly -- locate the exact lines by searching for the hardcoded values.
  </action>
  <verify>
    <automated>grep -rn "#FFFFFF\|#E8E0D8\|#8B7355" app/\(tabs\)/index.tsx src/components/recipes/RecipeCard.tsx app/\(public\)/recipe/\[id\].tsx app/\(public\)/index.tsx app/\(tabs\)/recipes/\[id\].tsx && echo "FAIL: hardcoded colors remain" || echo "PASS: no hardcoded colors"; grep -rn "fontFamily.*BricolageGrotesque" app/ src/components/public/ | grep -v "_layout" && echo "FAIL: raw font strings remain" || echo "PASS: no raw font strings"; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Zero hardcoded hex values (#FFFFFF, #E8E0D8, #8B7355) in target files. Zero raw BricolageGrotesque_700Bold strings outside font-loading _layout.tsx. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn '#FFFFFF\|#E8E0D8\|#8B7355' app/(tabs)/index.tsx src/components/recipes/RecipeCard.tsx app/(public)/recipe/[id].tsx app/(public)/index.tsx app/(tabs)/recipes/[id].tsx` returns no matches
2. `grep -rn "fontFamily.*BricolageGrotesque" app/ src/components/public/` returns no matches (except _layout.tsx font loading)
3. `npx tsc --noEmit` passes with no errors
4. `npx jest --passWithNoTests` passes
</verification>

<success_criteria>
- All hardcoded hex colors replaced with token imports in target files
- fontFamilyDisplayBold token exists and is used in all 8 places across 2 files
- noPhotoBg and noPhotoIcon tokens exist and replace all local const declarations
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-audit-cleanup/11.1-01-SUMMARY.md`
</output>
