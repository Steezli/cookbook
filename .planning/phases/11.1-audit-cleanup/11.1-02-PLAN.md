---
phase: 11.1-audit-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["11.1-01"]
files_modified:
  - app/(tabs)/_layout.tsx
  - app/(public)/index.tsx
  - src/components/nav/MobileTabBar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Stale comment in (tabs)/_layout.tsx correctly says 4 tab routes, not 5"
    - "The as any type assertion in (public)/index.tsx is removed and TypeScript compiles"
    - "MobileTabBar and WebSidebar both use router.navigate for scan navigation"
  artifacts:
    - path: "app/(tabs)/_layout.tsx"
      provides: "Corrected comment"
      contains: "4 tab routes"
    - path: "src/components/nav/MobileTabBar.tsx"
      provides: "Unified scan navigation method"
      contains: "router.navigate"
  key_links:
    - from: "src/components/nav/MobileTabBar.tsx"
      to: "/scan route"
      via: "router.navigate('/scan')"
      pattern: "router\\.navigate.*scan"
---

<objective>
Fix three small tech debt items from the v1.1 audit: correct a stale comment, remove an unnecessary type assertion, and unify scan navigation methods.

Purpose: Close remaining audit items that are independent of the token extraction work.
Output: Three files with targeted single-line fixes.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-audit-cleanup/11.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale comment and remove type assertion</name>
  <files>app/(tabs)/_layout.tsx, app/(public)/index.tsx</files>
  <action>
Two independent single-line fixes:

1. **app/(tabs)/_layout.tsx line 26:** Change the comment that says "5 tab routes" to "4 tab routes". Only index, my-recipes, family, and profile are registered tab routes (scan is handled separately via router.navigate).

2. **app/(public)/index.tsx:** Find the line with `pathname: '/(public)/recipe/[id]' as any` and remove `as any`. The typed route already exists in `.expo/types/router.d.ts`. Note: Plan 01 already modified the imports and const declarations at the top of this file -- the `as any` is further down in the router.push call.
  </action>
  <verify>
    <automated>grep -n "5 tab routes" app/\(tabs\)/_layout.tsx && echo "FAIL: stale comment" || echo "PASS: comment fixed"; grep -n "as any" app/\(public\)/index.tsx && echo "FAIL: as any still present" || echo "PASS: as any removed"; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>Comment says "4 tab routes". No "as any" in (public)/index.tsx. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Unify scan navigation to router.navigate</name>
  <files>src/components/nav/MobileTabBar.tsx</files>
  <action>
In src/components/nav/MobileTabBar.tsx line 41, change:
  `router.push("/scan")`
to:
  `router.navigate("/scan")`

This matches WebSidebar.tsx line 75 which already uses router.navigate("/scan").

Rationale (from Phase 9 decision 09-04): router.navigate() resolves from root navigator on all platforms and prevents duplicate stack entries. router.push() would stack multiple scan screens on repeated taps.

Do NOT change WebSidebar.tsx -- it already uses the correct method.
  </action>
  <verify>
    <automated>grep -n 'router\.push.*/scan' src/components/nav/MobileTabBar.tsx && echo "FAIL: still using push" || echo "PASS: using navigate"; grep -n 'router\.navigate.*/scan' src/components/nav/MobileTabBar.tsx src/components/nav/WebSidebar.tsx</automated>
  </verify>
  <done>Both MobileTabBar.tsx and WebSidebar.tsx use router.navigate("/scan") for scan navigation.</done>
</task>

</tasks>

<verification>
1. `grep "5 tab routes" app/(tabs)/_layout.tsx` returns no matches
2. `grep "as any" app/(public)/index.tsx` returns no matches
3. `grep "router.push" src/components/nav/MobileTabBar.tsx` returns no scan-related matches
4. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Stale comment corrected from "5 tab routes" to "4 tab routes"
- Type assertion removed, TypeScript compiles without errors
- MobileTabBar and WebSidebar both use router.navigate for scan
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-audit-cleanup/11.1-02-SUMMARY.md`
</output>
