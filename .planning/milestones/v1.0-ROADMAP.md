# Roadmap: Cookbook (Family Recipe Vault)

**Created:** 2026-02-02

All v1 requirements map to exactly one phase.

## Phases (v1)

| # | Phase | Goal | Requirements | Success Criteria |
|---|-------|------|--------------|------------------|
| 1 | Foundation (Identity + Family + Privacy) | Safe invite-only family spaces with enforced recipe visibility | AUTH-01..04, FAM-01..05, VIS-01..02 | 4 | 6/6 | Complete   | 2026-03-02 | Users can build and manage a recipe library | REC-01..05, COLL-01..02, SRCH-01..02 | 4 |
| 3 | Scan to Draft (Photo → Structured) | Photos become editable recipe drafts that users can finalize | SCAN-01..04 | 4 |
| 4 | Trust + Collaboration (Units + Social) | Units display correctly and families can discuss/rate recipes | UNIT-01..02, SOC-01..02 | 4 |
| 5 | Public + Monetization (Optional) | Public browsing exists and monetization stays non-intrusive | PUB-01..02, MON-01..02 | 4 |
| 6 | Fix Scan Integration (Gap Closure) | All scan-to-recipe flows work end-to-end with correct auth, params, and DB wiring | SCAN-01, SCAN-03, SCAN-04 | 5 |
| 7 | Native Compatibility for Scan UI (Gap Closure) | DraftReview/DraftEditor render and navigate correctly on iOS/Android native | SCAN-03, SCAN-04 | 5 |
| 8 | Home Navigation + Photo Polish (Gap Closure) | Recipes are discoverable from home and photo features are complete | REC-04 | 4 |

---

## Phase Details

### Phase 1: Foundation (Identity + Family + Privacy)

**Goal:** Safe invite-only family spaces with enforced recipe visibility.

**Requirements:**
AUTH-01, AUTH-02, AUTH-03, AUTH-04,
FAM-01, FAM-02, FAM-03, FAM-04, FAM-05,
VIS-01, VIS-02

**Success criteria:**
1. User can sign up, log in, stay logged in, and log out.
2. User can create a family space, invite another user, and that user can join successfully.
3. A non-member cannot access family recipes via API/data queries (privacy is enforced server-side).
4. A recipe's visibility (private/family/public) is a first-class field and is respected everywhere.

### Phase 2: Recipe Core (Create + Organize + Find)

**Goal:** Users can build and manage a recipe library.

**Requirements:**
REC-01, REC-02, REC-03, REC-04, REC-05,
COLL-01, COLL-02,
SRCH-01, SRCH-02

**Plans:** 6 plans

Plans:
- [x] 02-01-PLAN.md — Recipe CRUD (database schema, API, UI screens)
- [x] 02-02-PLAN.md — Photo attachments (storage, upload, display)
- [x] 02-03-PLAN.md — Collections (personal & family recipe organization)
- [x] 02-04-PLAN.md — Tags & Search (filtering and search UI)
- [x] 02-05-PLAN.md — Gap closure: collection membership management UI
- [x] 02-06-PLAN.md — Gap closure: recipe list photo thumbnails

**Plans:** 7 plans

Plans:
- [x] 02-01-PLAN.md — Recipe CRUD (database schema, API, UI screens)
- [x] 02-02-PLAN.md — Photo attachments (storage, upload, display)
- [x] 02-03-PLAN.md — Collections (personal & family recipe organization)
- [x] 02-04-PLAN.md — Tags & Search (filtering and search UI)
- [x] 02-05-PLAN.md — Gap closure: collection membership management UI
- [x] 02-06-PLAN.md — Gap closure: recipe list photo thumbnails
- [x] 02-07-PLAN.md — Gap closure: UAT fixes (delete button, edit refresh, tag filter, photo display)

**Verification:** `gaps_found` (6/8 must-haves verified) — see `.planning/phases/02-recipe-core-create-+-organize-+-find/02-recipe-core-create-+-organize-+-find-VERIFICATION.md`

**Note:** Major collection grouping gap closed. Only remaining gap is photo thumbnails in recipe lists, which was intentionally deferred per plan decision for performance optimization.

**Success criteria:**
1. User can create/edit/delete a recipe with ingredients + steps and optional metadata.
2. User can attach photos to a recipe and see thumbnails in lists.
3. User can tag recipes and group them into collections.
4. User can search by title/tags and browse their accessible recipe lists.

### Phase 3: Scan to Draft (Photo → Structured)

**Goal:** Photos become editable recipe drafts that users can finalize.

**Requirements:**
SCAN-01, SCAN-02, SCAN-03, SCAN-04

**Plans:** 7 plans

Plans:
- [x] 03-01-PLAN.md — Photo upload and job system
- [x] 03-02-PLAN.md — OCR integration and confidence scoring
- [x] 03-03-PLAN.md — Draft editing interface
- [x] 03-04-PLAN.md — Job status tracking and error handling
- [x] 03-05-PLAN.md — Gap closure: Authentication integration
- [x] 03-06-PLAN.md — Scan navigation integration (route group, hub, and navigation entry points)
- [x] 03-07-PLAN.md — Multi-image upload support for multi-page recipes

**Status:** Complete and verified (2026-02-06)
**Verification:** `passed` (8/8 must-haves verified) — see `.planning/phases/03-scan-to-draft-photo-to-structured/03-scan-to-draft-photo-to-structured-VERIFICATION.md`

**Note:** All SCAN requirements implemented with enhancements: multi-image upload for multi-page recipes, scan navigation integration, and authentication gap closure. Multi-photo database migration requires application to remote Supabase.

**Success criteria:**
1. User can upload a photo to start a scan job and see progress/status.
2. A scan produces a structured draft (ingredients/steps/units) and preserves raw extracted text.
3. User can correct any extracted field and save the result as a normal recipe.
4. Failed scans show actionable errors and support retry without losing user work.

### Phase 4: Trust + Collaboration (Units + Social)

**Goal:** Units display correctly and families can discuss/rate recipes.

**Requirements:**
UNIT-01, UNIT-02,
SOC-01, SOC-02

**Plans:** 6/6 plans complete

Plans:
- [x] 04-01-PLAN.md — Database migration (comments, ratings, unit preference, RLS, triggers)
- [x] 04-02-PLAN.md — Unit conversion engine and ingredient parser (TDD)
- [x] 04-03-PLAN.md — Comments feature (API, threaded UI, recipe detail integration)
- [x] 04-04-PLAN.md — Ratings feature (API, star rating UI, recipe detail integration)
- [x] 04-05-PLAN.md — Unit preference and display (settings, conversion display, parse+confirm)
- [ ] 04-06-PLAN.md — Gap closure: parse+confirm dismiss button and verification of partial gaps

**Success criteria:**
1. Ingredients can store canonical amounts/units when possible while preserving as-entered text for ambiguous cases.
2. User can set metric/imperial preference and recipes display accordingly.
3. Family/private recipes support family-only comments; public recipes support public comments.
4. Users can rate recipes with half-star increments and see averages/counts.

### Phase 6: Fix Scan Integration (Gap Closure)

**Goal:** All scan-to-recipe flows work end-to-end with correct auth, params, and DB wiring.

**Requirements:**
SCAN-01, SCAN-03, SCAN-04

**Gap Closure:** Closes gaps from v1.0 milestone audit (2026-03-02)

**Plans:** 7 plans

Plans:
- [x] 06-01-PLAN.md — Service-layer fixes (auth wiring, user filter, retry delegation, convertToRecipe columns, mapScoreToStatus, Supabase client)
- [x] 06-02-PLAN.md — UI/navigation fixes (useLocalSearchParams, DraftEditor expo-router navigation, DraftManager share fix)
- [x] 06-03-PLAN.md — UAT gap closure: draft ID mismatch (getDraftByJobId, updateDraftStatus types, session guards)
- [x] 06-04-PLAN.md — UAT gap closure: retry broken profiles join and scan_jobs status constraint
- [x] 06-05-PLAN.md — UAT gap closure: wire DraftEditor into draft route (convert, discard, share actions)
- [x] 06-06-PLAN.md — UAT gap closure: fix DraftEditor scroll so DraftManager actions are reachable
- [x] 06-07-PLAN.md — UAT gap closure: fix DraftManager dialog overlays (Save as Recipe, Discard) with inline styles

**Success criteria:**
1. `getCurrentUserId()` returns the authenticated user's ID — real-time scan status updates fire correctly.
2. `retryScanJob` calls `supabase.rpc()` correctly (not nested in `.update()` value).
3. `draft/[id].tsx` uses `useLocalSearchParams()` — draft review screen loads the correct draft.
4. `convertToRecipe` uses correct column names (`owner_user_id`, no `status`/`scan_draft_id`) — saving draft as recipe succeeds.
5. `DraftEditor.tsx` uses expo-router for navigation — no `window.location.href` crashes on native.
6. Draft loads correctly when navigating from scan job list (getDraftByJobId resolves scan_job_id to draft).
7. Retry on failed scan jobs succeeds without profiles join error.
8. scan_drafts status values match DB CHECK constraint (ready, needs_review, enhanced).

### Phase 7: Native Compatibility for Scan UI (Gap Closure)

**Goal:** DraftReview/DraftEditor render and navigate correctly on iOS/Android native.

**Requirements:**
SCAN-03, SCAN-04

**Gap Closure:** Closes P1 integration and flow gaps from v1.0 milestone audit (2026-03-04)

**Plans:** 3 plans

Plans:
- [x] 07-01-PLAN.md — DraftReview RN conversion + dead code cleanup (AIAssistant, ScanDraft type, scan hub)
- [x] 07-02-PLAN.md — DraftEditor + DraftManager RN conversion with Modal dialogs
- [ ] 07-03-PLAN.md — UAT gap closure: fix DraftEditor save (job ID vs draft primary key mismatch in updateDraftRecipe)

**Success criteria:**
1. DraftReview.tsx renders on iOS/Android without crash — no web-only HTML elements (`div`, `span`, `input`, `textarea`, `button`, `pre`, `h1`–`h3`).
2. DraftEditor.tsx renders on iOS/Android without crash — all web HTML replaced with React Native components.
3. "Back to Scans" navigation uses `router.back()` — no `window.history.back()`.
4. All styling uses React Native StyleSheet — no Tailwind `className` on non-overlay elements.
5. Dead code removed: orphaned `ScanDraft` type and `getScanDraft` in `scan-service.ts`, dead "Review Draft" button in `app/(scan)/index.tsx`.
6. DraftEditor field editing saves successfully — `updateDraftRecipe` receives draft primary key, not job ID.

### Phase 8: Home Navigation + Photo Polish (Gap Closure)

**Goal:** Recipes are discoverable from home and photo features are complete.

**Requirements:**
REC-04

**Gap Closure:** Closes P2/P3 integration gaps and REC-04 partial from v1.0 milestone audit (2026-03-04)

**Success criteria:**
1. Home screen has a navigation link to `/recipes` for authenticated users — recipe features are discoverable.
2. DraftReview displays the original scan photo (not a placeholder stub).
3. ScanPhotoUpload fires `onUploadComplete` callback — scan job list refreshes immediately after upload.
4. Recipe list views show photo thumbnails for recipes with attached photos.

### Phase 5: Public + Monetization

**Goal:** Public browsing exists and monetization stays non-intrusive.

**Requirements:**
PUB-01, PUB-02,
MON-01, MON-02

**Gap Closure:** Closes unsatisfied Phase 5 requirements from v1.0 milestone audit (2026-03-04)

**Success criteria:**
1. Users can browse public recipes (list + detail) without needing family membership.
2. Public recipes show attribution to the user who added them (family attribution is explicitly not default).
3. Ads appear only on public browsing screens and do not cause disruptive layout shifts.
4. Scanning can be gated by a subscription entitlement (even if billing UI is minimal in v1).
