---
phase: 02-recipe-core-create-+-organize-+-find
plan: "06"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/features/recipes/photos.ts
  - app/recipes/index.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - "User can attach photos to a recipe and see thumbnails in recipe lists"
    - "Thumbnail fetching does not require N+1 per recipe (single additional query is acceptable)"
    - "Thumbnails respect access control by relying on existing RLS for recipe_photos and storage"
  artifacts:
    - path: src/features/recipes/photos.ts
      provides: "Thumbnail URL + list-friendly fetch helpers"
      exports: ["getThumbnailUrl"]
    - path: app/recipes/index.tsx
      provides: "Recipe list UI displaying thumbnails when available"
      contains: "recipeCard"
  key_links:
    - from: app/recipes/index.tsx
      to: src/features/recipes/photos.ts
      via: "getThumbnailUrl + list thumbnail map"
      pattern: "(getThumbnailUrl|thumbnail)"
    - from: src/features/recipes/photos.ts
      to: "recipe_photos"
      via: "supabase query for recipe_id in [ids]"
      pattern: "\\.from\\(\"recipe_photos\"\\)"
---

<objective>
Close Phase 02 photo browsing gap by showing recipe photo thumbnails in recipe list views.

Purpose: Improve browse/recognition flow and satisfy REC-04’s “thumbnails in lists” requirement.
Output: List-friendly thumbnail fetch helper + updated recipe list UI to render thumbnails.
</objective>

<execution_context>
@/Users/elinicholson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-recipe-core-create-+-organize-+-find/02-recipe-core-create-+-organize-+-find-VERIFICATION.md
@.planning/phases/02-recipe-core-create-+-organize-+-find/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add list-friendly thumbnail fetch helper</name>
  <files>src/features/recipes/photos.ts</files>
  <action>
Extend `src/features/recipes/photos.ts` with a helper that fetches *first* photo per recipe in one query:

- Add function:
  - `getFirstRecipePhotos(recipeIds: string[]): Promise<Record<string, RecipePhoto>>`
    - If `recipeIds` empty, return empty object early.
    - Query `recipe_photos` selecting: `recipe_id, storage_path, sort_order, created_at`
    - Filter with `.in("recipe_id", recipeIds)`
    - Order by `recipe_id` then `sort_order` ascending (and/or created_at as tie-breaker)
    - Reduce into `{ [recipe_id]: firstPhoto }`

- Add function (optional but ergonomic):
  - `getRecipeThumbnailUrlMap(recipeIds: string[], width: number = 120): Promise<Record<string, string>>`
    - Uses `getFirstRecipePhotos` + `getThumbnailUrl(photo.storage_path, width)`

Do not change existing upload/delete behavior.
  </action>
  <verify>cat src/features/recipes/photos.ts | grep -E "getFirstRecipePhotos|getRecipeThumbnailUrlMap"</verify>
  <done>Photo module exposes a helper to fetch first-photo thumbnails for many recipes efficiently</done>
</task>

<task type="auto">
  <name>Task 2: Render thumbnails in recipe list</name>
  <files>app/recipes/index.tsx</files>
  <action>
Update `app/recipes/index.tsx` to display a thumbnail on each recipe card when available:

- Import `Image` from `react-native`.
- Import the new helper from `src/features/recipes/photos.ts` (or import `getFirstRecipePhotos` + `getThumbnailUrl`).
- Add state: `thumbnailByRecipeId: Record<string, string>` (or `Record<string, RecipePhoto>`).
- In `loadRecipes()` after fetching recipes:
  - Build `recipeIds = data.map(r => r.id)`
  - Fetch thumbnail map with **one** call (not per recipe).
  - Store in state.
- In `renderItem`:
  - If `thumbnailByRecipeId[item.id]` exists, render `<Image ... />` on the left of the card.
  - Keep layout pleasant on small screens (thumbnail ~72–88px square).
- Remove/replace the “thumbnails deferred” comment at top once implemented.

Keep list behavior the same otherwise (search/filters unchanged).
  </action>
  <verify>cat app/recipes/index.tsx | grep -E "(Image|thumbnailByRecipeId|getRecipeThumbnailUrlMap|getThumbnailUrl)"</verify>
  <done>Recipe list shows photo thumbnails where available, without per-recipe fetch calls</done>
</task>

</tasks>

<verification>
Manual testing:

1. Create recipe A with a photo and recipe B without photos.
2. Open recipe list → A shows thumbnail, B shows no thumbnail, layout remains aligned.
3. Confirm thumbnails still respect access boundaries (e.g., sign out / other user cannot load private recipe photo URLs).
</verification>

<success_criteria>
- [ ] Thumbnails show in `app/recipes/index.tsx` for recipes with photos
- [ ] Thumbnail fetch uses 1 additional query for the whole list (no N+1)
- [ ] No regressions to search/filter UI
</success_criteria>

<output>
After completion, create `.planning/phases/02-recipe-core-create-+-organize-+-find/02-06-SUMMARY.md`
</output>

