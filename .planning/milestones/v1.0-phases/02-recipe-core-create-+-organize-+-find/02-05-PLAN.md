---
phase: 02-recipe-core-create-+-organize-+-find
plan: "05"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/recipes/[id].tsx
  - app/collections/[id].tsx
  - src/features/collections/api.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "User can add a recipe to a collection from recipe detail"
    - "User can remove a recipe from a collection from recipe detail or collection detail"
    - "User can manage collection membership without needing hidden/implicit flows"
    - "Collection membership actions respect RLS (only allowed recipes/collections are visible and mutable)"
  artifacts:
    - path: app/recipes/[id].tsx
      provides: "Recipe detail collection membership management UI"
      contains: "Add to Collection"
    - path: app/collections/[id].tsx
      provides: "Collection detail recipe list with membership controls"
      contains: "removeRecipeFromCollection"
    - path: src/features/collections/api.ts
      provides: "Recipe↔collection functions used by UI"
      exports: ["getCollections", "getRecipeCollections", "addRecipeToCollection", "removeRecipeFromCollection"]
  key_links:
    - from: app/recipes/[id].tsx
      to: src/features/collections/api.ts
      via: "getRecipeCollections + add/remove calls"
      pattern: "(getRecipeCollections|addRecipeToCollection|removeRecipeFromCollection)"
    - from: app/collections/[id].tsx
      to: src/features/collections/api.ts
      via: "removeRecipeFromCollection + add path for adding recipes"
      pattern: "(removeRecipeFromCollection|addRecipeToCollection)"
---

<objective>
Close Phase 02 collection UX gaps by adding clear, user-facing recipe↔collection management.

Purpose: Make “organize recipes into collections” fully usable and obvious in daily flows.
Output: Updated recipe detail + collection detail screens with add/remove and lightweight bulk add.
</objective>

<execution_context>
@/Users/elinicholson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-recipe-core-create-+-organize-+-find/02-recipe-core-create-+-organize-+-find-VERIFICATION.md
@.planning/phases/02-recipe-core-create-+-organize-+-find/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make recipe detail show and manage collection membership</name>
  <files>app/recipes/[id].tsx, src/features/collections/api.ts</files>
  <action>
Update `app/recipes/[id].tsx` so the collection UI is not “add-only”:

- Load current membership for the recipe via `getRecipeCollections(recipe.id)` and store as a set of collection ids.
- Render collections as a toggle list:
  - If recipe is already in a collection: show “Added” state and allow **Remove** (call `removeRecipeFromCollection(collection.id, recipe.id)`).
  - If not in a collection: allow **Add** (call `addRecipeToCollection(collection.id, recipe.id)`).
- Keep the UI owner-gated (existing `isOwner` check), but make the state explicit (users should see which collections contain the recipe).
- Handle duplicate-add gracefully (API already throws “Recipe is already in this collection”).

If `src/features/collections/api.ts` needs a tiny helper for UI ergonomics, add a small, testable helper such as:
- `collectionIdSet(collections: Collection[]): Set<string>` OR
- `getRecipeCollectionIds(recipeId: string): Promise<string[]>` (thin wrapper over `getRecipeCollections`)

Keep changes additive; do not change schema or RLS.
  </action>
  <verify>cat app/recipes/[id].tsx | grep -E "(getRecipeCollections|removeRecipeFromCollection)"</verify>
  <done>Recipe detail clearly shows membership and supports add/remove without confusion</done>
</task>

<task type="auto">
  <name>Task 2: Add lightweight “bulk add” flow in collection detail</name>
  <files>app/collections/[id].tsx</files>
  <action>
Update `app/collections/[id].tsx` to support adding multiple recipes into the current collection from within the collection screen:

- Add a small “Add recipes” section for owners:
  - A `TextInput` search field (title substring match) and a scrollable list of matching accessible recipes.
  - For each search result: show an “Add” button (or checkbox + “Add selected”).
- Implement by reusing existing recipes query logic:
  - Prefer importing `searchRecipes` from `src/features/recipes/search.ts` to avoid duplicating query code.
- When adding:
  - Call `addRecipeToCollection(collection.id, recipeId)` for each selected recipe.
  - Update the local `recipes` state to include newly added recipes (or re-load via `loadCollection()`).
- Ensure you exclude recipes already in the collection from the candidate list (use `recipes.map(r => r.id)`).

Scope note: “Bulk” here means “add multiple without navigating into each recipe”; it can be implemented as repeated inserts (no new batch API required).
  </action>
  <verify>cat app/collections/[id].tsx | grep -E "(searchRecipes|addRecipeToCollection)"</verify>
  <done>Owner can add multiple recipes to a collection from the collection detail screen</done>
</task>

</tasks>

<verification>
Manual testing (end-to-end):

1. Create a recipe and a collection.
2. Open recipe detail → add recipe to collection → confirm success.
3. Re-open recipe detail → confirm collection shows as “added” → remove it → confirm removed.
4. Open collection detail → add 2+ recipes via bulk add UI → confirm they appear in list.
5. Confirm remove still works from collection detail.
</verification>

<success_criteria>
- [ ] Recipe detail shows collection membership state and supports add/remove
- [ ] Collection detail provides a bulk add affordance for owners
- [ ] No schema/RLS changes required; actions rely on existing `collection_recipes` policies
</success_criteria>

<output>
After completion, create `.planning/phases/02-recipe-core-create-+-organize-+-find/02-05-SUMMARY.md`
</output>

