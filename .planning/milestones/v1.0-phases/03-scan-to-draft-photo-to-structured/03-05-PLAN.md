---
phase: 03-scan-to-draft-photo-to-structured
plan: 05
type: execute
wave: 3
depends_on: []
files_modified: [src/features/scans/DraftEditor.tsx, src/features/scans/DraftManager.tsx, src/features/scans/DraftReview.tsx]
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Scan components use actual user authentication instead of hardcoded placeholders"
    - "User context properly retrieved from existing auth system"
    - "RLS policies are enforced through proper user identification"
    - "Scan features work correctly for authenticated users only"
    - "Multi-user environment properly supported"
  artifacts:
    - path: "src/features/scans/DraftEditor.tsx"
      provides: "Draft editing with proper user context"
      contains: "useSession hook and session.user.id usage"
    - path: "src/features/scans/DraftManager.tsx"
      provides: "Draft management with proper user context"
      contains: "useSession hook and session.user.id usage"
    - path: "src/features/scans/DraftReview.tsx"
      provides: "Draft review with proper user context"
      contains: "useSession hook and session.user.id usage"
  key_links:
    - from: "src/features/scans/DraftEditor.tsx"
      to: "src/features/auth/session.tsx"
      via: "useSession hook"
      pattern: "import.*useSession.*from.*session"
    - from: "src/features/scans/DraftManager.tsx"
      to: "src/features/auth/session.tsx"
      via: "useSession hook"
      pattern: "import.*useSession.*from.*session"
    - from: "src/features/scans/DraftReview.tsx"
      to: "src/features/auth/session.tsx"
      via: "useSession hook"
      pattern: "import.*useSession.*from.*session"
---

<objective>
Fix authentication integration in Phase 3 scan components by replacing hardcoded user ID placeholders with actual user context from the existing auth system.

Purpose: Enable proper multi-user functionality and ensure RLS policies are enforced by using real authentication instead of placeholder values.
Output: All scan components properly integrated with auth system, working in multi-user environment with security controls enforced.
</objective>

<execution_context>
@/Users/elinicholson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-scan-to-draft-photo-to-structured/03-VERIFICATION.md
@.planning/phases/01-foundation-identity-family-privacy/01-03-SUMMARY.md

# Gap Details from Verification

Critical authentication integration gap found:
- DraftEditor.tsx (lines 35, 69): Uses 'current-user-id' placeholder
- DraftManager.tsx (lines 41, 62, 90): Uses 'current-user-id' placeholder  
- DraftReview.tsx (line 20): Uses 'current-user-id' placeholder

# Existing Auth System Pattern

From Phase 1 auth implementation:
- SessionProvider provides session context throughout app
- useSession() hook returns { session, isLoading }
- User ID accessed via session.user.id
- Pattern already used throughout app in recipes/[id].tsx, recipes/index.tsx, etc.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DraftEditor.tsx to use actual authentication</name>
  <files>src/features/scans/DraftEditor.tsx</files>
  <action>
1. Add import: import { useSession } from "@/features/auth/session";
2. Add const { session, isLoading: authLoading } = useSession(); inside component
3. Replace 'current-user-id' placeholder on line 35 with session.user.id
4. Replace 'current-user-id' placeholder on line 69 with session.user.id
5. Add loading state check: if (authLoading) return <ActivityIndicator />;
6. Add authentication check: if (!session) return <Text>Please log in to edit drafts</Text>;
7. Ensure all userId parameters use session.user.id consistently
</action>
  <verify>
1. Check that 'current-user-id' placeholders are completely removed
2. Verify useSession hook is imported and used correctly
3. Confirm auth loading and session checks are present
4. TypeScript compiles without errors: npm run typecheck
</verify>
  <done>DraftEditor component properly integrates with auth system, using real user ID and handling auth states</done>
</task>

<task type="auto">
  <name>Task 2: Update DraftManager.tsx to use actual authentication</name>
  <files>src/features/scans/DraftManager.tsx</files>
  <action>
1. Add import: import { useSession } from "@/features/auth/session";
2. Add const { session, isLoading: authLoading } = useSession(); inside component
3. Replace 'current-user-id' placeholder on line 41 with session.user.id (saveAsDraft function)
4. Replace 'current-user-id' placeholder on line 62 with session.user.id (saveAsRecipe function)
5. Replace 'current-user-id' placeholder on line 90 with session.user.id (discardDraft function)
6. Add loading state check: if (authLoading) return <ActivityIndicator />;
7. Add authentication check: if (!session) return <Text>Please log in to manage drafts</Text>;
8. Ensure all userId parameters use session.user.id consistently
</action>
  <verify>
1. Check that all 'current-user-id' placeholders are completely removed
2. Verify useSession hook is imported and used correctly
3. Confirm auth loading and session checks are present
4. TypeScript compiles without errors: npm run typecheck
5. Verify all three functions (saveAsDraft, saveAsRecipe, discardDraft) use session.user.id
</verify>
  <done>DraftManager component properly integrates with auth system, using real user ID for all draft operations</done>
</task>

<task type="auto">
  <name>Task 3: Update DraftReview.tsx to use actual authentication</name>
  <files>src/features/scans/DraftReview.tsx</files>
  <action>
1. Add import: import { useSession } from "@/features/auth/session";
2. Add const { session, isLoading: authLoading } = useSession(); inside component
3. Replace 'current-user-id' placeholder on line 20 with session.user.id
4. Add loading state check: if (authLoading) return <ActivityIndicator />;
5. Add authentication check: if (!session) return <Text>Please log in to review drafts</Text>;
6. Ensure userId parameter uses session.user.id consistently
</action>
  <verify>
1. Check that 'current-user-id' placeholder is completely removed
2. Verify useSession hook is imported and used correctly
3. Confirm auth loading and session checks are present
4. TypeScript compiles without errors: npm run typecheck
</verify>
  <done>DraftReview component properly integrates with auth system, using real user ID for draft loading</done>
</task>

</tasks>

<verification>
1. Run TypeScript compilation: npm run typecheck - no errors expected
2. Search codebase for any remaining 'current-user-id' placeholders: rg "current-user-id" src/
3. Test that all scan components properly handle authentication states
4. Verify RLS policies will now be enforced with real user IDs
5. Confirm no hardcoded user IDs remain in scan system
</verification>

<success_criteria>
- All 'current-user-id' placeholders replaced with session.user.id
- All scan components properly handle authentication loading and error states
- Users can only access their own scan drafts (RLS enforced)
- Multi-user environment properly supported
- TypeScript compilation succeeds without errors
- No hardcoded user identifiers remain in scan system
</success_criteria>

<output>
After completion, create `.planning/phases/03-scan-to-draft-photo-to-structured/03-scan-to-draft-photo-to-structured-05-SUMMARY.md`
</output>