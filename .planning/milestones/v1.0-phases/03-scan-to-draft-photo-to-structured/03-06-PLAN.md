---
phase: 03-scan-to-draft-photo-to-structured
plan: 06
type: execute
wave: 1
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified: ["app/(scan)/_layout.tsx", "app/(scan)/index.tsx", "app/(scan)/draft/[id].tsx", "app/index.tsx"]
autonomous: false
must_haves:
  truths:
    - "Users can access scan features from main app navigation"
    - "Users can upload photos and see scan progress"
    - "Users can review and edit scan drafts"
    - "Complete scan-to-recipe workflow is accessible end-to-end"
    - "Navigation flows properly between scan and other app sections"
  artifacts:
    - path: "app/(scan)/_layout.tsx"
      provides: "Scan route group layout with proper navigation stack"
      min_lines: 10
    - path: "app/(scan)/index.tsx"
      provides: "Main scan hub with upload and job tracking"
      min_lines: 40
    - path: "app/(scan)/draft/[id].tsx"
      provides: "Draft review screen integration"
      min_lines: 15
    - path: "app/index.tsx"
      provides: "Main navigation entry point to scan features"
      min_lines: 5 (modified)
  key_links:
    - from: "app/index.tsx"
      to: "/(scan)"
      via: "Link component for authenticated users"
      pattern: "Link.*href=\"/\\(scan)\""
    - from: "app/(scan)/index.tsx"
      to: "src/features/scan/ScanPhotoUpload"
      via: "Component import and usage"
      pattern: "import.*ScanPhotoUpload"
    - from: "app/(scan)/index.tsx"
      to: "src/features/scan/ScanJobList"
      via: "Component import and usage"
      pattern: "import.*ScanJobList"
    - from: "app/(scan)/draft/[id].tsx"
      to: "src/features/scans/DraftReview"
      via: "Component prop passing"
      pattern: "DraftReview.*draftId"
---

<objective>
Create core navigation structure to make scan features accessible through the app, enabling UAT testing of the Phase 3 scan-to-draft workflow.

Purpose: The scan component suite is fully implemented but completely inaccessible due to missing app routes. This plan creates the minimal navigation structure needed to expose the scan features to users and enable end-to-end testing.

Output: Functional scan navigation with upload, job tracking, and draft review accessible from main app.
</objective>

<execution_context>
@/Users/elinicholson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/scan-navigation-gap.md
@.planning/debug/scan-navigation-gap-analysis.md
@.planning/ROADMAP.md

# Existing scan components
@src/features/scan/ScanPhotoUpload.tsx
@src/features/scan/ScanJobList.tsx
@src/features/scans/DraftReview.tsx

# Current app structure
@app/index.tsx
@app/(auth)/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Create scan route group layout</name>
  <files>app/(scan)/_layout.tsx</files>
  <action>Create expo-router Stack layout for scan routes:
- Import Stack from expo-router
- Export default ScanLayout component
- Configure Stack with headerTitle "Recipe Scanner"
- Follow existing pattern from app/(auth)/_layout.tsx
- Ensure proper integration with app navigation system</action>
  <verify>File exists and contains valid Stack configuration with proper header</verify>
  <done>Scan route group layout is functional and properly integrated</done>
</task>

<task type="auto">
  <name>Create main scan hub page</name>
  <files>app/(scan)/index.tsx</files>
  <action>Create main scan hub integrating existing components:
- Import React Native components (View, Text, StyleSheet, Link)
- Import ScanPhotoUpload from @/features/scan/ScanPhotoUpload (React Native version)
- Import ScanJobList from @/features/scan/ScanJobList (React Native version)
- Create centered layout with title "Recipe Scanner"
- Render ScanPhotoUpload component for photo upload functionality
- Render ScanJobList component for job status tracking
- Add navigation link to draft management: Link href="/(scan)/draft/[id]"
- Apply consistent styling matching app design patterns
- Ensure components are properly spaced and functional</action>
  <verify>File exists, imports correct components, renders upload and job list functionality</verify>
  <done>Users can access scan hub, upload photos, and track job progress</done>
</task>

<task type="auto">
  <name>Create draft review route</name>
  <files>app/(scan)/draft/[id].tsx</files>
  <action>Create dynamic route for draft review:
- Import DraftReview component from @/features/scans/DraftReview
- Export default function accepting params object with id string
- Pass draftId prop to DraftReview component: {params.id}
- Follow expo-router dynamic route pattern
- Ensure proper TypeScript typing for params
- Handle edge cases for missing/invalid IDs if needed</action>
  <verify>File exists and properly integrates DraftReview with dynamic routing</verify>
  <done>Users can access draft review screen for specific scan drafts</done>
</task>

<task type="auto">
  <name>Add scan navigation entry point</name>
  <files>app/index.tsx</files>
  <action>Add scan link to main app navigation:
- In authenticated section (after Families link, before logout)
- Add: Link href="/(scan)" with text "Scan Recipes"
- Use existing styles.link pattern for consistency
- Ensure link only appears for authenticated users (session exists)
- Maintain proper spacing and navigation flow
- Do not modify unauthenticated section or existing functionality</action>
  <verify>app/index.tsx contains scan navigation link for authenticated users</verify>
  <done>Main app navigation includes entry point to scan features</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete scan navigation structure with upload, job tracking, and draft review accessible from main app</what-built>
  <how-to-verify>
    1. Start the mobile app (npm run start or expo start)
    2. Log in with an authenticated user account
    3. Verify "Scan Recipes" link appears in main navigation
    4. Click "Scan Recipes" link - should navigate to scan hub
    5. Test photo upload functionality - should show upload interface
    6. Verify job list appears (may be empty initially)
    7. Navigate back to main app - should work properly
    8. Test navigation flow multiple times to ensure stability
  </how-to-verify>
  <resume-signal>Type "approved" if navigation works correctly, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- Scan route group layout follows expo-router patterns
- Main scan hub integrates existing React Native components correctly
- Draft review route properly handles dynamic IDs and component props
- Main app navigation includes scan entry point for authenticated users
- Navigation flows between scan and other app sections without errors
- All components render and are functional in the mobile app context
- Authentication is properly handled (scan routes only accessible to authenticated users)
</verification>

<success_criteria>
Users can access the complete scan-to-draft workflow through app navigation:
1. Authenticated users see "Scan Recipes" link in main navigation
2. Scan hub loads with photo upload and job tracking functionality
3. Photo upload interface is functional and integrates with existing scan system
4. Job list displays current scan jobs and status
5. Draft review is accessible through dynamic routing
6. Navigation flows properly between scan features and main app
7. UAT testing can proceed without navigation blockers
</success_criteria>

<output>
After completion, create `.planning/phases/03-scan-to-draft-photo-to-structured/03-scan-to-draft-photo-to-structured-06-SUMMARY.md`
</output>