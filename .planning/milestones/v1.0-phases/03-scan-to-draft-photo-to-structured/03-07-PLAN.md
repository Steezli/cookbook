---
phase: 03-scan-to-draft-photo-to-structured
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [src/features/scan/ScanPhotoUpload.tsx, src/features/scan/scan-upload.ts, src/features/scan/scan-photos.ts, src/features/scan/scan-service.ts]
autonomous: true
must_haves:
  truths:
    - "User can select multiple images for one scan job"
    - "User can see all selected images in a preview/gallery"
    - "User can reorder and remove individual images"
    - "All images upload together as a single scan job"
    - "Proper loading states for multi-image upload"
    - "Backward compatibility with single image uploads"
    - "Scan job tracking handles multiple photos per job"
  artifacts:
    - path: "src/features/scan/ScanPhotoUpload.tsx"
      provides: "Multi-image selection and gallery UI"
      contains: "ImagePicker with multiple selection, image gallery, drag reorder, remove functionality"
    - path: "src/features/scan/scan-upload.ts"
      provides: "Multi-image upload processing"
      contains: "uploadScanPhotosWithValidation function"
    - path: "src/features/scan/scan-photos.ts"
      provides: "Multiple photo upload to storage"
      contains: "uploadScanPhotos function, batch photo operations"
    - path: "src/features/scan/scan-service.ts"
      provides: "Multi-photo job creation and tracking"
      contains: "createMultiPhotoScanJob, ScanJob type updated for multiple photos"
  key_links:
    - from: "src/features/scan/ScanPhotoUpload.tsx"
      to: "src/features/scan/scan-upload.ts"
      via: "uploadScanPhotosWithValidation function"
      pattern: "uploadScanPhotosWithValidation"
    - from: "src/features/scan/scan-upload.ts"
      to: "src/features/scan/scan-photos.ts"
      via: "uploadScanPhotos function"
      pattern: "uploadScanPhotos"
    - from: "src/features/scan/scan-photos.ts"
      to: "src/features/scan/scan-service.ts"
      via: "createMultiPhotoScanJob function"
      pattern: "createMultiPhotoScanJob"
---

<objective>
Implement multi-image upload support for recipe scanning while maintaining backward compatibility with single image uploads.

Purpose: Enable users to upload multiple recipe pages as a single scan job, addressing the UAT gap where real recipes often span multiple pages.
Output: Enhanced photo upload component with multi-image selection, gallery preview, reordering, and batch upload processing.
</objective>

<execution_context>
@/Users/elinicholson/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap from UAT testing
@src/features/scan/ScanPhotoUpload.tsx
@src/features/scan/scan-upload.ts
@src/features/scan/scan-photos.ts
@src/features/scan/scan-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ScanPhotoUpload for multi-image selection</name>
  <files>src/features/scan/ScanPhotoUpload.tsx</files>
  <action>
Update ScanPhotoUpload.tsx to support multiple image selection and gallery display:

1. Update state management:
   - Replace `selectedImage` with `selectedImages: ImagePicker.ImagePickerAsset[]`
   - Add `selectedImagesOrder: string[]` for reordering functionality

2. Modify image selection:
   - Update ImagePicker.launchImageLibraryAsync to set `allowsMultiple: true`
   - Handle multiple asset selection in `handleImageSelect`

3. Create image gallery component:
   - Render grid of selected images with thumbnails
   - Add remove button for each image (red X overlay)
   - Add reorder functionality using long-press and drag
   - Show image count and total file size

4. Update upload flow:
   - Disable upload when no images selected
   - Show "Upload [X] Photos & Start Scan" button text
   - Maintain backward compatibility when only 1 image selected

5. Preview and management:
   - Show image previews in a scrollable horizontal gallery
   - Allow tapping to view full-size image in modal
   - Add drag handles for reordering (visual indicators)

Key changes:
- Replace single image preview with multi-image gallery
- Add drag-and-drop reordering using `react-native-draggable-flatlist`
- Maintain all existing error handling and validation logic
- Preserve styling consistency with existing UI
</action>
  <verify>Component compiles without TypeScript errors and shows multi-image selection UI</verify>
  <done>User can select multiple images, see gallery preview, reorder images, and remove individual images</done>
</task>

<task type="auto">
  <name>Task 2: Update upload service for batch processing</name>
  <files>src/features/scan/scan-upload.ts, src/features/scan/scan-photos.ts</files>
  <action>
Create multi-image upload functions while preserving single-image compatibility:

1. In scan-upload.ts:
   - Add `uploadScanPhotosWithValidation` function for multiple files
   - Maintain existing `uploadScanPhotoWithValidation` for backward compatibility
   - Add batch validation (total size limits, per-file validation)
   - Create `MultiScanUploadResult` interface extending `ScanUploadResult`
   - Add progress tracking for batch uploads

2. In scan-photos.ts:
   - Add `uploadScanPhotos` function for batch upload to storage
   - Update file naming to include sequence numbers (e.g., `timestamp-001.jpg`, `timestamp-002.jpg`)
   - Return array of `{ photoUrl: string; storagePath: string }` objects
   - Implement parallel upload with controlled concurrency (max 3 simultaneous)
   - Add individual file error handling without failing entire batch

3. Batch processing logic:
   - Validate all images before starting uploads
   - Show progress like "Uploading 2 of 5 photos..."
   - Continue uploading other images if one fails
   - Roll back storage uploads if job creation fails

4. Error handling:
   - Per-image errors with specific file identification
   - Partial success handling (some photos uploaded successfully)
   - Clear error messages for batch operations

Maintain React Native compatibility and all existing single-image functionality.
</action>
  <verify>Functions compile and can handle array of files for batch upload</verify>
  <done>Multiple images can be uploaded simultaneously with proper validation and progress tracking</done>
</task>

<task type="auto">
  <name>Task 3: Update scan service for multi-photo jobs</name>
  <files>src/features/scan/scan-service.ts</files>
  <action>
Extend scan service to handle multiple photos per job while maintaining backward compatibility:

1. Update ScanJob type:
   - Change `photo_url: string` to `photo_urls: string[]`
   - Add `primary_photo_url: string` for backward compatibility
   - Add `photo_count: number` field for quick reference

2. Create `createMultiPhotoScanJob` function:
   - Accept array of photo URLs
   - Set primary_photo_url to first photo for existing UI compatibility
   - Store all photo URLs in photo_urls array
   - Increment photo_count based on array length

3. Update existing functions for backward compatibility:
   - Modify `createScanJob` to handle single photo but store in array format
   - Add `getJobPhotos` function to retrieve all photo URLs
   - Update `deleteScanPhoto` to handle multiple photos per job

4. Database compatibility layer:
   - Create view or computed property for `photo_url` from `photo_urls[0]`
   - Ensure existing queries continue to work
   - Add migration path for existing single-photo jobs

5. Update real-time subscriptions:
   - Ensure `subscribeToJob` and `subscribeToUserJobs` work with multi-photo jobs
   - Maintain all existing event handling

6. API compatibility:
   - Keep existing `photo_url` field populated for backward compatibility
   - Add `photo_urls` field for new multi-photo functionality
   - Ensure all existing components continue to work without changes

This approach allows existing UI components to work unchanged while enabling new multi-photo features.
</action>
  <verify>Service functions compile and can create jobs with multiple photos while maintaining backward compatibility</verify>
  <done>Scan jobs can store multiple photos, existing single-photo jobs continue to work, and new multi-photo jobs are properly tracked</done>
</task>

</tasks>

<verification>
1. Test multi-image selection flow:
   - Launch image picker and select multiple photos
   - Verify gallery displays all selected images
   - Test reordering by dragging images
   - Test removing individual images
   - Verify file count and size display

2. Test upload flow:
   - Upload multiple images and verify progress indicators
   - Check that all images are uploaded to storage
   - Verify scan job is created with multiple photo URLs
   - Test error handling with invalid images

3. Test backward compatibility:
   - Upload single image using existing flow
   - Verify existing single-photo jobs still work
   - Check that job list displays correctly for both single and multi-photo jobs
   - Test that existing draft review interface works

4. Test scan job tracking:
   - Create multi-photo scan job
   - Verify job appears in job list with correct status
   - Test real-time updates for multi-photo jobs
   - Ensure retry/cancel functionality works

5. Performance validation:
   - Test with 5+ images to ensure UI remains responsive
   - Verify upload progress tracking works correctly
   - Check memory usage doesn't spike excessively
</verification>

<success_criteria>
1. Multi-image selection works with expo-image-picker allowing multiple photo selection
2. Gallery preview shows all selected images with thumbnails, reordering, and removal options
3. Batch upload processes all images with progress tracking and proper error handling
4. Scan jobs store multiple photo URLs while maintaining backward compatibility
5. Existing single-image upload flow continues to work unchanged
6. All UAT Test 2 requirements are satisfied for multi-page recipe support
7. Performance remains acceptable with up to 10 images per scan job
</success_criteria>

<output>
After completion, create `.planning/phases/03-scan-to-draft-photo-to-structured/03-07-SUMMARY.md` documenting the multi-image upload implementation and UAT gap closure.
</output>