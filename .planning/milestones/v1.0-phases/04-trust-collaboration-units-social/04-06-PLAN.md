---
phase: 04-trust-collaboration-units-social
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/recipes/create.tsx
  - app/recipes/[id]/edit.tsx
autonomous: false
gap_closure: true
requirements: [UNIT-01, UNIT-02, SOC-01]

must_haves:
  truths:
    - "User can dismiss/reject parsed ingredient preview and submit plain text without canonical fields"
    - "Parse preview clearly shows amount, unit, and ingredient breakdown with Confirm and Dismiss buttons"
    - "Ambiguous measurements show 'Ambiguous measurement' message in create/edit parse preview"
    - "Family admin can see and use delete button on other users' comments on family recipes"
  artifacts:
    - path: "app/recipes/create.tsx"
      provides: "Parse+confirm with explicit dismiss/reject in ingredient entry"
      contains: "dismissParse"
    - path: "app/recipes/[id]/edit.tsx"
      provides: "Parse+confirm with explicit dismiss/reject in ingredient entry"
      contains: "dismissParse"
  key_links:
    - from: "app/recipes/create.tsx"
      to: "handleSubmit"
      via: "dismissed ingredient has no canonical fields"
      pattern: "!ing.confirmed.*!ing.parsed"
---

<objective>
Close 4 verification gaps from Phase 04 verification: add explicit dismiss/reject button to parse+confirm preview on ingredient entry forms, and verify the existing parse preview rendering, ambiguous indicator, and family admin moderation via human checkpoint.

Purpose: The verifier identified 4 truths as partial/needs_human. One requires a small code change (adding explicit reject/dismiss), and the other three require human visual/runtime verification of already-implemented features.

Output: Updated create.tsx and edit.tsx with dismiss button, plus human verification of all 4 gaps.
</objective>

<execution_context>
@/Users/elinicholson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elinicholson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trust-collaboration-units-social/04-05-SUMMARY.md
@.planning/phases/04-trust-collaboration-units-social/04-VERIFICATION.md
@app/recipes/create.tsx
@app/recipes/[id]/edit.tsx
@src/features/comments/CommentThread.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit dismiss/reject button to parse+confirm ingredient preview</name>
  <files>app/recipes/create.tsx, app/recipes/[id]/edit.tsx</files>
  <action>
    Add a `dismissParse` function to both create.tsx and edit.tsx that clears the parsed state for a given ingredient index, allowing the user to explicitly reject the parse result and submit plain text.

    In both files, add this function alongside the existing `confirmParse`:

    ```typescript
    function dismissParse(index: number) {
      const updated = [...ingredients];
      updated[index] = { text: updated[index].text, parsed: undefined, confirmed: false };
      setIngredients(updated);
    }
    ```

    In the parse preview JSX section (the `{ingredient.parsed && !ingredient.confirmed && (...)}` block), add a "Dismiss" button next to the existing "Confirm" button. The Dismiss button should:
    - Appear as a secondary/outline-style button (gray border, no fill) next to the blue Confirm button
    - Call `dismissParse(index)` on press
    - Have text "Dismiss" in gray color

    Add two new styles to both files' StyleSheet:
    - `dismissButton`: `{ borderWidth: 1, borderColor: '#999', paddingHorizontal: 12, paddingVertical: 6, borderRadius: 6, marginLeft: 8 }`
    - `dismissButtonText`: `{ color: '#666', fontSize: 12, fontWeight: '500' }`

    Also update the `parsePreview` style in both files to wrap the buttons properly: add `flexWrap: 'wrap'` and change `justifyContent` to keep the text on top and buttons on the right.

    The parse preview section should render as:
    ```
    [Parse message text]              [Dismiss] [Confirm]
    ```

    This makes the reject flow explicit: user sees parsed result, can Confirm (saves canonical fields), Dismiss (clears parse, submits as plain text), or simply ignore (same as Dismiss behavior on submit since confirmed defaults to false).
  </action>
  <verify>
    Run `npx tsc --noEmit 2>&1 | grep -E "create\.tsx|edit\.tsx"` to verify no new TypeScript errors in modified files. Verify the dismissParse function exists in both files by grepping: `grep -n "dismissParse" app/recipes/create.tsx app/recipes/\[id\]/edit.tsx`.
  </verify>
  <done>
    Both create.tsx and edit.tsx have a "Dismiss" button in the parse preview that clears the parsed state. User can explicitly reject a parse result. Unconfirmed ingredients save as plain text without canonical fields (existing behavior, now with explicit UX path).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 4 gap closures (parse preview, dismiss, ambiguous indicator, family admin moderation)</name>
  <files>app/recipes/create.tsx, app/recipes/[id]/edit.tsx, src/features/comments/CommentThread.tsx</files>
  <action>
    Human verification checkpoint. No code changes — verify the following features visually and at runtime:
    1. Parse preview rendering (amount + unit + ingredient breakdown) with Confirm and Dismiss buttons
    2. Dismiss/reject flow (new Dismiss button clears parsed state)
    3. Ambiguous measurement indicator in create/edit forms
    4. Family admin comment moderation (delete button visible and functional for admin, hidden for non-admin)
  </action>
  <verify>
    **Test 1: Parse Preview Display**
    1. Go to Create Recipe screen
    2. Type "2 cups flour" in an ingredient field and tap outside (blur)
    3. EXPECTED: Blue preview bar appears showing "Parsed: 2 cups flour" with [Dismiss] and [Confirm] buttons

    **Test 2: Confirm Parse Flow**
    1. From Test 1, tap "Confirm"
    2. EXPECTED: Preview disappears, green "Parsed and confirmed" text appears

    **Test 3: Dismiss Parse Flow**
    1. Type "1 tsp vanilla" in another ingredient and blur
    2. Tap "Dismiss"
    3. EXPECTED: Preview disappears, no confirmation text. Submit the recipe.
    4. EXPECTED: Recipe saves successfully. When viewing the recipe, this ingredient shows as plain text (no unit conversion available)

    **Test 4: Ambiguous Measurement**
    1. Type "a pinch of salt" in an ingredient and blur
    2. EXPECTED: Preview shows "Ambiguous measurement — will be preserved as-is" with [Dismiss] and [Confirm] buttons

    **Test 5: Edit Form Parse Preview**
    1. Open Edit screen for a recipe
    2. Pre-parsed ingredients should show "Parsed and confirmed"
    3. Add a new ingredient, type "3 tbsp butter", blur
    4. EXPECTED: Same parse preview with Dismiss/Confirm as create form

    **Test 6: Family Admin Moderation**
    1. Log in as a family admin user
    2. Navigate to a family recipe with comments from other family members
    3. EXPECTED: Delete button visible on ALL comments (not just own)
    4. Tap Delete on another user's comment
    5. EXPECTED: Confirmation dialog appears, comment is deleted

    **Test 7: Non-Admin Cannot Delete Others' Comments**
    1. Log in as a regular family member (not admin)
    2. Navigate to same family recipe
    3. EXPECTED: Delete button only visible on own comments, NOT on others'
  </verify>
  <done>All 7 tests pass. Parse preview renders clearly, dismiss/reject works, ambiguous indicator appears, and family admin moderation functions correctly.</done>
</task>

</tasks>

<verification>
- Parse preview shows clear amount/unit/ingredient breakdown on both create and edit forms
- Dismiss button clears parsed state; unconfirmed ingredients save as plain text
- Ambiguous measurements show appropriate indicator in parse preview
- Family admin can delete any comment on family recipes
- Non-admin family members can only delete their own comments
- TypeScript compilation passes for modified files
</verification>

<success_criteria>
All 4 verification gaps from 04-VERIFICATION.md are closed:
1. Parse preview renders clearly with Confirm AND Dismiss buttons
2. User can reject parsed ingredient (explicit Dismiss or implicit ignore)
3. Ambiguous measurement indicator appears in create/edit forms
4. Family admin moderation works correctly at runtime
</success_criteria>

<output>
After completion, create `.planning/phases/04-trust-collaboration-units-social/04-06-SUMMARY.md`
</output>
